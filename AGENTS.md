# 仓库协作约定

## 仓库与权限
- 所有产品代码、前端、客户端、服务端和功能交付归入 `https://gitee.com/xinghetech/yike-ai2026`，最终合并目标为 `main`。
- `main` 是唯一长期主干。已授权的小范围串行改动可直接在最新主干处理；较大或并行工作使用短期 `codex/<主题>` 分支。复用合适的既有隔离工作树，不为每个零碎改动新建分支。
- 保留其他贡献者的修改，不自行切换、覆盖、清理他人的工作树。
- `yike-ai` 保留为只读历史治理仓；2026-09-13 仅获准本地入口纠错，不迁移或归档历史文档。
- 当前实施范围以 `AUTHORITY.md` 最新批准决定为准。本文件不授权新增外部消息、支付、凭据访问或生产部署。

## 按任务读取
- 开始新的实施批次时确认 `AUTHORITY.md` 的当前范围及 `docs/V02_IMPLEMENTATION_TASKBOOK.md` 对应任务；同一批次续接只查变化和当前卡点。
- Git协作/集成问题查 `docs/REPOSITORY_WORKFLOW.md`、`docs/INTEGRATION_STATUS.md`；架构、迁移、部署或界面变更读取相应文档，不每次加载全套。
- 状态查询只读适用记录，不触发开发、审核或构包流程。

## 可用试用版优先
- 当前试用收尾：文字描述业务→确认搜索目标→真实公开搜索→查看原文与候选→复制联系草稿→手工反馈。
- 语音、复杂多轮、全平台深采及自动外联等后续项不阻塞本次试用；完整V0.2规划保留，不把试用通过写成全量完成。
- 已批准范围内的小修复直接推进，不重复设计确认；实质范围、权限或不可逆决策变化才询问。
- 复用R3/R4及已有模块；局部UI调整不要求重新生成设计图或重建视觉系统。

## 验证与交付
- 按可交付批次做定向验证和必要独立审核。审核绑定具体差异，需求与质量可在一次独立审核中覆盖；修复后核对受影响差量。
- 源码、依赖、配置及相关运行条件未变化时复用原证据，注明版本和局限；新变化、失败或未解决风险才触发补测。
- 准备实际交付候选时统一构包，允许合并多个相关批次；文档或无关模块变化不触发全量测试或重复构包。
- 已授权且无生产访问的本地检查失败，先定位并修复本次改动引起的问题，不逐步等待批准；真实权限或用户选择缺失时才停在该步骤。
- 真实任务、平台、生产与客户效果分别验收；不以测试、页面、提交或模型调用数量证明商业成功。
- 安全底线保留：凭据保护、租户隔离、任务/版本绑定、原文真实性和外部操作授权。
- 更新唯一任务台账，保留真实失败与适用版本。交付说明只列入口、可验收动作、版本、证据及剩余缺口，不重复生成整套材料。
