# 意客 AI 客户试用产品权威绑定

状态：`CUSTOMER_PILOT_ACTIVE / APPROVED_FOR_IMPLEMENTATION`

本分支是独立的客户试用产品实现线，继承既有 B 站＋抖音发现 MVP 的可复用代码，但不改变其主仓权威、平台范围或运行结论。

## 当前受控试用范围

首版面向有实际交付能力的服务商，首个验证行业为展台设计搭建。客户通过文字描述服务能力、地域、项目偏好和排除项；系统提供经人工复核导入的机会证据、匹配理由和联系草稿，并记录跟进反馈。

四个用户页面：

1. 业务画像：描述并确认服务、目标客户、地域、偏好和排除项；每次修改产生画像版本。
2. 机会列表：显示机会状态、更新时间、来源健康度和匹配摘要。
3. 机会详情：显示原文、证据、来源入口、限制条件和联系草稿；不自动发送。
4. 跟进反馈：记录适配判断、联系状态、回复、访谈和报价事实。

后台使用 Codex 与商机研究 Skill 进行研究，人工复核后导入。AI 开发商机池仅供自用，不进入客户空间。

## 下一版完整获客目标（2026-09-09 用户要求）

用户要求产品能够对外演示和收费，并明确补齐软件登录与平台连接、真实采集、多平台持续监控、AI 意向与证据、真实触达、回复及跟进。缺失能力必须进入实施与验收清单，现有四页研究包导入流程仅是复用基础与中间阶段，不能作为完整获客版的最终交付。

- 下一版范围、工程任务与发布规则：[V0.2 完整获客版计划](docs/V02_COMMERCIAL_RELEASE_PLAN.md)。
- 多平台研究 Skill、连接器与调度边界：[多平台 Skill 方案](docs/V02_MULTIPLATFORM_SKILL_PLAN.md)。
- 八入口蓝白左导航、Logo 与设计图：[设计入口](design/README.md)。用户选定最终设计图后再用 image-to-code 实现，配合 ui-ux-pro-max 统一组件规范。
- 用户已确认取得 MediaCrawler 代码商业授权；内部保留依赖溯源，对外统一使用意客AI及业务名称，不展示底层引擎名称，不重复索取该授权。
- 新版范围不代表功能已实现。触达必须经用户确认具体对象、内容和渠道，验证实际发送与回复，不把草稿复制、人工登记或模型生成当作发送成功。本次规划提交不执行任何对外消息。

现有 CP-01～CP-06 继续记录受控试用的实际进度。新版的功能验收与客户试用部署验收分别记录；计划和设计图不豁免数据库、安全、真实环境或独立审核要求。迁入基线与差异见 [交接说明](docs/V02_DESIGN_HANDOFF.md)。

## 数据与隔离

- 正式客户数据库使用 PostgreSQL；本地实验可使用显式标记的 SQLite 适配器。
- 客户空间、业务画像、推荐状态和跟进记录按 tenant_id 隔离。
- 公共来源按平台和外部 ID 去重；内容变更保存新版本，重复观察保存 observation，不制造假新增。
- 同一项目的多个来源只标记疑似关联并保留原证据，不能静默合并或删除。
- 任务使用数据库唯一约束、幂等键和过期租约，防止重复执行和旧执行者回写。
- 密钥、Cookie、Profile、验证码和私密会话不进入 Git、数据库或日志。
- 客户端使用服务端签发的短期签名会话令牌；数据库仅允许私网应用角色访问，RLS 是第二层防线，不把可写会话变量当作客户端身份凭据。

## 交付边界

本分支先完成本地空库和受控试用流程，再申请目标环境部署。上线前必须提供部署 SHA、HTTPS、身份隔离、日志脱敏、备份恢复、回滚和真实手机验收证据。自动外联、平台风控绕过、付款和公开推广不在首版执行路径内。

## 审核要求

主实现者不得给自己的提交做最终审核。架构审核、代码审核和质量验收必须由独立 Agent 完成，审核结论绑定具体提交；修复后必须复审，存在上线阻断问题时不得发布。
