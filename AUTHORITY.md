# 意客AI V0.2 产品与开发权威

状态：`V02_PRODUCT_SCOPE_ACTIVE / APPROVED_FOR_IMPLEMENTATION`
更新：2026-09-09，按用户要求将本轮讨论落实为当前开发目标；新增功能尚未完成。

## 当前产品目标

面向有实际交付能力的服务商，交付能够对外演示、由客户实际使用并收费的完整获客产品：

**产品登录与平台连接 → 业务画像与任务 → 多平台真实采集 → 持续监控 → AI 意向与原文证据 → 商机复核 → 确认后真实触达 → 回复与跟进。**

首个验证行业沿用展台设计搭建；领域模型保留其他服务行业扩展能力。客户付费价值在于持续发现可行动的新需求及推进联系，不以菜单数量、试用留言或虚构业绩证明需求。

当前产品范围为 [V0.2 完整获客版计划](docs/V02_COMMERCIAL_RELEASE_PLAN.md)。登录、采集、监控、真实触达与回复等缺失能力必须开发并验收，不能因为现有代码缺失而改成交付人工导入与复制草稿。四页研究包试用流程是现有实现基础，不再限定最终产品范围。

## 文档权威与执行入口

1. 用户最新明确要求与本文件：决定目标、边界及冲突处理。
2. [完整获客版计划](docs/V02_COMMERCIAL_RELEASE_PLAN.md)：功能范围、交付标准与商业验证。
3. [多平台 Skill 方案](docs/V02_MULTIPLATFORM_SKILL_PLAN.md) 与 [设计入口](design/README.md)：技术和视觉规范，不能改变上层功能范围。
4. [V0.2 实施任务书](docs/V02_IMPLEMENTATION_TASKBOOK.md)：唯一的当前开发任务状态、优先级、依赖和验收证据台账。
5. [客户试用计划](docs/CUSTOMER_PILOT_PLAN.md)、现有运行手册与历史审核：说明已有实现及受控运行路径，不覆盖上述新目标。

设计交接、历史评审和旧双平台 MVP 文档保留溯源。旧文档中的“四页即交付”“仅人工导入”“系统永不执行发送”等范围限制，仅描述旧实现，不能用于取消 V0.2 已纳入的功能。运行说明必须与实际代码一致，未实现的功能不能写成可执行命令或已通过验收。

## 功能与品牌决定

- 八个一级入口：商机工作台、业务画像、线索采集、监控任务、商机库、触达中心、跟进记录、账号与授权。
- 监控必须覆盖多个平台，首发适配按多平台方案中的小红书、抖音、B站、知乎及公开网站/社区建议组合拆解。平台支持范围分别验收，不能只做一个平台后宣布完成。
- 采用通用商机研究 Skill＋平台 Skill＋实际连接器＋可靠任务系统；Skill 要版本化入库，不能把提示词文档或平台菜单当成运行能力。
- 用户已确认取得 MediaCrawler 代码商业授权，按该前提复用，不重复索取。客户页面、普通状态及销售材料统一意客AI与业务名称；内部保留依赖和许可溯源。
- 真实触达属于当前开发范围，必须绑定用户确认的对象、内容版本、渠道与连接身份；任一变化使原确认失效。未经确认不得发送，结果未知先核对，禁止盲目重试。
- 复制草稿、打开来源、人工登记、平台发送回执、实际回复分别记录，不能相互冒充。

## 视觉与交付形态

保留蓝白科技风、Y Logo 和左侧分组导航，减少装饰卡片与重复说明。先交付[完整页面图册与关键状态](design/v02-suite/README.md)，由用户统一确认后再用 image-to-code 实现，配合 ui-ux-pro-max 统一组件规范；不能只确认一页便自行定稿其他页面。当前 R1 为 20 张核心页面及操作弹窗图，复用状态另有清单，全部待用户确认。

用户确认记录须绑定设计版本、图片摘要及修改意见；没有明确确认不能把设计标为 APPROVED。按图开发后，用相同视口与状态的真实运行截图逐页对照；实质布局或流程变更先更新设计再确认。后台协议、数据库、Skill 与执行器不因等待整套视觉确认而停工。

完整付费版按现行计划交付 Windows 客户端与服务端能力。桌面界面及本地平台执行器通过受认证的 HTTPS 服务端 API 工作，正式客户数据仍在私网 PostgreSQL；客户端不能持有数据库连接、管理员凭据或服务端签名密钥。平台会话按客户、设备、平台和账号隔离，在专用私有存储中管理。本地关机/离线时停止实际采集并如实显示离线，不能记为无新增；不默认承诺关机后仍采集。Windows 安装、运行、取消/恢复、更新/回退需真实环境验收。

## 必须继承的数据与安全约束

- 正式客户数据库使用 PostgreSQL；SQLite 仅用于明确标记的本地实验，不成为第二套正式客户库。
- 客户空间、画像、任务、候选、商机、触达与跟进按服务端身份解析的 tenant_id 隔离；自用商机不进入客户空间。
- 已确认画像按版本绑定任务与判断；来源按平台及外部 ID 去重，保留内容版本和观察。跨平台同项目可标记关联，没有证据不能按昵称合并为同一人。
- 任务使用唯一约束、幂等、租约与执行代次。有效结果、游标和任务完成在短事务内校验执行者并提交；事务不跨浏览器、模型或网络等待。
- 密钥、Cookie、Profile、验证码和私密会话不进入 Git、业务数据库、日志或导出；平台登录态置于隔离的专用凭据/会话存储。
- 平台验证码及验证挑战由用户按可用流程处理；限流、验证和权限不足分别记录，不以绕过验证码、限流或权限控制作为实现路径。真实采集与确认后发送仍按当前范围开发并验收。
- 应用与管理员数据库连接严格分离；运行时 env 禁止携带管理员连接。迁移、provision 及当前受信研究包导入仅在管理员路径执行。
- 桌面采集结果通过新增候选 API 上传，由服务端校验身份、画像与复核记录；设备不能自报 APPROVED 或 reviewer 绕过复核，也不能执行管理员导入命令。
- 数据库仅允许私网非超级用户应用角色访问；保留 v3 租户 RLS、Origin 防护、日志脱敏和服务端签名会话。RLS 不代替身份认证或生产 ACL，桌面接入不能通过关闭 Origin 检查或启用生产开发登录桥接实现。

以上是必须保持或补齐的约束，不表示执行代次、整包事务、正常登录及新连接器已经实现；真实状态统一见任务书。

## 开发、验收与发布

当前工作重心是完成 V0.2 产品闭环。CP-01～CP-05 的历史实现和检查作为复用基础，CP-06 作为部署验收子门禁保留；缺少生产服务器、域名或 HTTPS 时，继续完成不依赖这些条件的研发与本地验证。

M1 多平台首次采集、M2 持续监控与复核是阶段里程碑。M3 还须完成真实触达、回复跟进、Windows 交付和独立验收，才满足完整付费版定义。生产上线另须部署 SHA、HTTPS、数据库隔离、备份恢复、回滚和真实手机证据。不能用静态设计、文档、模拟数据或旧测试结果替代新增能力的真实验收。

主实现者不得给自己的提交做最终审核。架构审核、代码审核和质量验收必须由独立 Agent 完成，结论绑定具体提交；修复后复审，有发布阻断问题不得发布。当前文档调整不执行对外消息、支付、推广或生产部署。
