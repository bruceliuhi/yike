# R4 设计实现验收

result: passed

范围：已授权的 R4 六组前端增量及本轮明确列出的状态。此结果不表示全部 P01–P20 状态矩阵、生产业务、Windows 或完整 Goal 已完成。

用户已经确认 R4 并授权其余继续开发，随后同意将研究预算统一用“搜贝”表达。P06 据此修改，未将其记录为一次不存在的单独再确认。实现基线使用现有设计系统、平台原 Logo 与品牌资产，不更换页面风格。

## 对照方法

参考来自 [R4 设计图](design/v02-suite-r4/README.md)，运行图与参考在同一次图像工具输入中实际观察。主要 CSS 视口为 1484×1060；P10 原图为 1485×1059，保持原文件，不伪造像素完全一致。通过 Chromium 设备度量和密度得到同尺寸原始截图，没有用拼接或修改 DOM 修补画面。

P12 使用 [无客户条目的只读样例](docs/qa/ui-r4/screens/P12-matched-state.png) 对齐原设计的资料获取用途、31 字内容及只读状态；另保留可操作客户 TEST 状态。P02/P09/P10/P11 的 TEST 数据覆盖真实实现的条件分支，与原图的空集合/公共样例有所不同，明确区分布局判断与数据状态判断。没有宣称全部六页精确同状态像素复刻。逐项说明与文件哈希见 [视觉独立复核](docs/qa/ui-r4/VISUAL_REVIEW.md)。

## 五项视觉检查

| 面 | 检查结果 |
|---|---|
| 布局与密度 | 沿用 208px 导航、60px 顶栏、分栏证据、440px 抽屉；新增区块遵循既有主次布局。P09 减少顶部占高；P11 抽屉打开时让出背景证据宽度。 |
| 字体与文字层级 | 按项目设计 tokens 渲染，平台名 14px、状态 12px；不追随生成图中的字形变形。主标题、来源与辅助说明有明确层级。 |
| 颜色与表面 | 保持浅底、蓝色操作与细分隔线；证据浅蓝引用、未知与风险状态不涂为成功。减少重复说明和卡片。 |
| 资产与图标 | 使用仓库品牌及平台原 Logo；公共网站用现有 Globe 图标；不以平台图标代表已经连接或已完成搜索。 |
| 内容与交互状态 | 搜贝的估算/上限/实际分开；公开样例只读、客户 TEST 显著标记；采用建议后保留检查摘要，人工编辑使旧依据失效。 |

## 修复迭代

1. 初轮 P06 研究配置过高：收紧字段和辅助行，保留停止条件与高级上限。
2. P02 平台行信息偏弱：字号与间距统一到系统规范。
3. P09 顶部重复信息使覆盖表过低：完整关键词改为可展开，刷新并入快照行，覆盖表上移至约 y=504。完整词项仍可操作。
4. P11 来源/画像状态暴露内部枚举，抽屉压窄证据：改为中文状态，背景联系栏临时收起，原文依据可展开，关闭后恢复。
5. P12 发送配置挤压短句区、应用后建议依据消失：配置移到正文/教练之后并默认折叠；应用摘要绑定采用的确切文本、版本、用户与空间；公开样例改为 31 字单问题。
6. 截图工具曾产生 2.5 倍局部裁切：重置设备度量、导航并等待真实页面加载后重新捕获。无效裁切图未作为最终视觉通过证据；最终 PNG 在正常完整视口检查。

最新六组未见新增 P0/P1 视觉问题；发现的上述 P2 已修并复看。P09 的用量明细、P11 的长配置和 P12 客户态下方操作需要纵向滚动，属于当前长内容排版范围，不承诺全首屏展示。此轮未设计额外功能或制造业绩。

## 交互、适配与实现边界

[交互步骤及原始日志](docs/qa/ui-r4/README.md)记录估算失效、上限未知核对、新草稿交接、短句采用及保存。最终 [36 项视口](docs/qa/ui-r4/responsive.json) 覆盖 960×600 至 2560×1440，无横向溢出；最小尺寸抽屉底栏可见，恢复正常视口的关键词展开/页签鼠标点击通过。

本轮通过的是可接入服务的前端及 Mac 包构建/隔离启动。实际服务、收费、跨平台执行和 Windows 安装验收仍按 [唯一任务书](docs/V02_IMPLEMENTATION_TASKBOOK.md) 推进；未修改为 DONE。对应可操作预览和静态对照入口见 [验收图册](docs/qa/ui-r4/index.html)。
