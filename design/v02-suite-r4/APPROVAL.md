# R4 确认记录

日期：2026-09-09。

## 已确认：增量范围

用户原话：**“R4 确认”**。

当时可供确认的对象为 [R4_CHANGE_IMPACT_DRAFT](../R4_CHANGE_IMPACT_DRAFT.md) 中的六组增量范围与商业分期，新的六张视觉尚未生成。记录状态为 `SCOPE_CONFIRMED_VISUAL_PENDING`。已有 R3 实现授权继续有效。

## 历史：出图时六张视觉待确认

六张新图在本次范围确认之后生成，最终交付文件及其 SHA-256 见 [manifest](manifest.json)。图册中的 P12、P09、P06、P10、P11、P02 为六个不同页面组，**不是六选一的替代方案**。用户可以整套确认，也可以按页面提出调整；按页面 ID 与图片摘要登记选择，不能按图在聊天中的到达先后猜测。

当前六组视觉状态均为 `PENDING_USER_REVIEW`，没有记录用户批准这些新图。图内控件、数据和运行状态不是实现验收。图片生成文字及组件歧义按 [VISUAL_REVIEW](VISUAL_REVIEW.md) 校正。

待用户选择后补记：原话、时间、页面 ID、所选图 SHA-256、修改意见及实际实施范围。不要覆盖本次范围确认的历史，也不要把后续确认回填成生成前已批准。

## 最新：允许继续开发，统一搜贝用量

同日用户在本图册打开后先要求：**“可以统一用个搜贝之类的来衡量？”**，随后明确：**“其余没问题，可以继续开发”**。

据此，P12/P09/P10/P11/P02 及 P06 其余布局按当时最终图进入 `APPROVED_FOR_IMPLEMENTATION`。批准时的完整图片摘要保存在 [历史 manifest](history/manifest-before-visual-approval.json)，主图由提交 `40218e46ee844a55d65c6e69feaeb1465ebb163c` 固定，不能按后来生成图片的聊天顺序猜测选图。

研究用量前台统一为“搜贝”，显示预计、最多、实际；底层仍保留来源/时长/调用硬上限，进入高级设置。换算规则及售价未定，不能造充值、余额或扣费结果。P06 搜贝修订图按这条修改要求随后生成，原图保留在 [修改前 P06](history/R4-03-P06-before-soubei.png)，新图摘要见当前 manifest。此项是用户要求的实现修改，不声称新图又获得过一次独立明确批准，也不重复索取整套开发授权。

本次只授权继续功能开发与设计落地，不代表 R4 已完成、真实计量/执行服务已接通或产品已上线；真实验收继续进入原任务书。
