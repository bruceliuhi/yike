# R4 设计交付验证

2026-09-09。验证对象为本目录图册、六张设计原图与交接文档；未修改产品运行代码，未重跑无关的完整客户端/后端测试，也未重建安装包。

- 原图：6 张 PNG 的 SHA-256、实际宽高、参考截图 SHA-256 与 manifest 一致；HTTP 返回原始图像字节的摘要同样一致。P10 实际 1485 × 1059，其余 1484 × 1060。
- 本地引用：R4 文档、设计入口、影响评估和两份 R3 审计共 86 个 Markdown 本地引用存在。
- 图册内联 JavaScript：Node 24.19.0 `--check` 通过。
- 浏览器：Codex 内置浏览器后台独立页实际点击 P12/P09/P06/P10/P11/P02，六页深链接、标题、原图路径、自然尺寸与加载完成状态一致，无图片错误提示；实际切换并排对照、原始尺寸/适应窗口、上一组，P11 两图加载完成，已目视查看并排画面。
- 初次在前台共享页循环读取时记录到路由切换过渡态，且 `getByLabel` 精确匹配未找到下拉框；该批不作为通过证据。改用独立后台页、按实际 role 定位并等待目标图片/页面状态后，得到上述通过结果，没有修改产品或伪造截图。
- `bash scripts/secret_scan.sh`：clean。
- `git diff --check`：通过。首次暂存检查发现 coach 提示词两处行尾空白，已仅规范化空白；PNG 原字节不变，修正后重新检查。

R4 仍为六组主视觉待用户确认；静态页面验证不证明预算限制、短句服务、覆盖统计或时间线已实现。R3 审计报告中的 66 passed 只适用其中列明的既有前端套件，不能作为 R4 新能力通过的证据。

上述为 `40218e46ee844a55d65c6e69feaeb1465ebb163c` 的出图验证快照。随后用户批准其余设计继续开发、要求搜贝用量，P06 已按要求另生成修订图；原六图 manifest 与原 P06 保存在 history，新批准状态见 APPROVAL.md。原验证不追溯升级为新代码或修改后图片的运行验收。
