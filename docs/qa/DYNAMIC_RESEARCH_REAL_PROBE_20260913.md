# 动态研究真实探针与本地 Skill 对照

日期：2026-09-13。基线 `c8cfe8942063d6993919c83cf937591bda64c708`；两项修复 `c2169de`、`5c311bb`。这是内部真实服务调用验证，不是生产部署、真实客户验收或商机供给验收。

## 结论

原本地 Skill 的搜索覆盖、复盘规则与仓库副本一致，不能把恢复效果简化为扩充关键词。实际客户研究纵切暴露两个此前夹具未覆盖的边界错误，现已定向修复。**尚无一次本批真实模型→搜索→原文持久化→候选判断的完整成功结果。** 本地匿名小样也没有可立即交接机会；它不能代替此前授权社交平台深读的效果。

## 真实运行与两项修复

实际环境：Codex 0.153.4、豆包 `doubao-seed-2-1-turbo-260628`、Serper、匿名公开原文读取器；通过真实 runtime composition、签名 START、受限 PostgreSQL 与逐动作账本运行。客户/设备/画像与计量规则为隔离测试配置；没有真实客户登录、生产数据、真实定价或外联。凭据仅宿主内存加载，不进入客户端、子进程环境、仓库或证据文件。

确认上限为共享来源 18 次、10 分钟、模型 20 次；候选最多 12 条。画像文本建议 6 次查询/12 次读取，但实际宿主搜索上限为 10，不能把文字建议写成已强制的分项预算。

| 阶段 | 实际结果 | 判断 |
|---|---|---|
| 初次探针及只读诊断 | 0 MODEL/SEARCH/READ 入账，`STOPPED/runtime_failed` | Codex 顶层 `client_metadata` 携带会话标识，被原有敏感数据门禁正确拒绝；不是模型没有找到机会 |
| `c2169de` 后真实运行 | 72.07 秒；4 次模型、9 次搜索成功，首个 READ 为 UNKNOWN；候选/评估均 0 | 已解决模型启动；任务按未知结果停止，资源结算 UNCERTAIN；不重放同一请求 |
| `5c311bb` 后定向验证 | 实际 worker/MCP 合同及受限 PG 测试通过；未再次调用真实提供商 | 已修格式交接，不能追认上一轮 READ 成功或完整研究已通过 |

第一项修复仅删除保留的顶层传输元数据，再按原合同准入和转发。业务输入不递归删改，敏感门禁不放宽。新增测试先出现 502 RED；修复后 bridge/效果合同 **72 passed / 20.42s**。独立复核 `c2169de` Spec/Quality PASS。

第二项修复将读取器的原文对象，在进入持久结果校验前转换为 READ 状态结果包。此前实际合同要求结果包，而会话返回裸对象；测试 dispatcher 的直通夹具掩盖了问题。实际 worker/MCP 测试引入严格合同后先 RED，再修复；读取会话/网关/持久 dispatcher **21 passed / 3.27s**。新增真实受限 PG 测试覆盖会话→账本→候选发布/判断及缓存一次读取，**1 passed / 16 deselected / 3.63s**；外部页面与判断仍为测试夹具，不当真实商机。独立差量复核绑定 `5c311bb8acbf88e7469b0e1ecd5ab7440f2272db`：**Spec PASS / Quality PASS / Ready YES**，无阻断发现，另执行7项纯内存边界检查通过；没有重复提供商/数据库或全套测试。Ready只指修复可进入后续真实验证，不是可部署或质量已达标。

真实运行的 9 个查询依次为：企业知识库 RAG 开发 找团队 外包 报价；智能客服 系统 定制 开发 外包 公司；业务系统 智能体 工作流 对接 开发 找团队；AI 口语陪练 小程序 开发 外包 定制；私有部署 大模型 定制 开发 找团队 企业；AI 项目 外包 找人做 定制开发 社区 需求；V2EX 人工智能 外包 项目 需求；想做一个 AI 知识库 有没有靠谱的团队推荐；公司 要做 智能客服 系统 求推荐 外包。

结果多为供方 SEO、教程或旧帖。模型连续搜索 9 次后才读第一篇 `https://v2ex.com/t/1240685`（AI 试用期感慨），未体现强买方信号优先。该 READ 的原始结果未成功持久化，不能断言页面实际读取成功。模型 token 用量未从探针导出，不报告为零，也不为补计数重跑。

## 本地 Skill 匿名样本

同日并行只读小样：5 个查询、11 次页面打开尝试；8 次返回正文（5 篇详情、3 页列表）、3 次超时。**0 SEND_READY，2 REVIEW；没有证明净新增且可立即跟进的机会。**

- [心理 AI 平台整包需求](https://v2ex.com/t/1236030)：08-21、作者 leoexp，100 万为帖载整包金额；属于任务历史已有项目，采购主体/行业适配及本人更新未核全，不算新增。
- [龙岗数据集团 AI 智能体开发聚合页](https://www.qianlima.com/bid-629932926.html)：09-10、控制价 487,300 元；邀请制且已列邀请供应商，官方原文/未受邀参与路径未核实，不是可直接报名或联系的机会。

另排除过期客服外包、个人测试招募、供方报价讨论。电鸭与采招网访问失败不是“平台没有需求”；多数论坛评论只提取到位置，未取得完整作者回复。查询之一误写 `site.eleduck.com`，后续已用 `site:eleduck.com/posts` 更正，不能把首次尝试算成功的站内限定覆盖。

**这不是严格 A/B：**本地使用另一研究工具，实际查询、模型与预算不同，且都没有授权社交平台评论深读。无法据此比较哪个模型/Skill 更好，或推导市场无需求。

## 下一步与未完成边界

1. 保留本次 UNKNOWN 与原始失败；在独立的新验证批次中选择未覆盖的业务问题、排除已知重复入口，验证修复后的实际原文入账及候选判断。不盲目重放未知动作。
2. 改善搜索/阅读决策：出现供方噪声即切换买方表达或原生需求入口，尽早核原文与日期；不要先耗尽搜索预算再读弱相关帖子。仍需实际质量证据，不用更多测试替代。
3. 已知不可读页面与真正执行未知要分开。当前已知读取错误仍可能触发持久 UNKNOWN/停止；安全换源尚未实现，不能宣称可持续自适应研究。
4. 复用小红书、抖音、B站授权连接器补评论和本人更新深度；匿名网页工具不能替代它们。范围/时间/反证与去重规则继续保留。

未重复全量测试或构包；本批仅服务端 Python 变更，原 Mac 候选仍绑定此前 `b2aabc6`，不追认包含新服务端代码。未部署、未发消息、未证明跨行业有效供给、未完成 Windows/客户验收；完整 Goal ACTIVE。

本机只读原始证据：`/tmp/yike-dynamic-live.fnSNkh/evidence-after-telemetry-fix.json`，诊断同目录；本地小样 `/tmp/yike-local-skill-public-sample-20260913.md`。这些临时文件不是交付依赖，核心结论集中于本文，不提交凭据配置、完整运行转录或私人会话。

## 新方向真实批次（同日，绑定 aed17ff）

独立新任务，不续跑或重放此前 UNKNOWN。采样改为培训机构课程/口语陪练接入、录音标注和工业图像识别软件外包；画像显式列出此前重复项目排除，并要求尽早读原文。确认上限为12次共享来源、5分钟、14次模型，最多6条候选；文本建议最多4查询不是宿主独立硬限。客户/设备/计量规则仍为隔离测试配置，模型、搜索、读器、服务、受限PG均为实际实现。

结果：**39.16秒，5次模型成功、4次搜索成功、1次READ UNKNOWN，候选/判断均0**，最终 `STOPPED/effect_unknown`，资源 `UNCERTAIN`。验证断言如实失败（1 failed / 41.75s，含测试准备），没有刷新失败任务或二次 advance。诊断输出的 `code:null` 不是没有失败原因：正式DTO字段为 `stopCode:effect_unknown`。

| 查询 | 返回结果及实际动作 |
|---|---|
| 口语陪练 课程 接入 有偿开发 找团队 | 阿里云教程、工具商、课程/应用推广等，未形成买方原文 |
| 录音 数据 标注 外包 找团队 有偿 | 数据供应商、岗位、自动翻译讨论和工具计费页，不当中文采购证据 |
| 工业 图像识别 软件 外包 找团队 有偿开发 | 外包服务广告、平台推荐和技术介绍，未形成买方原文 |
| 电鸭社区 AI 外包 需求 2026 | 社区首页/分类/招聘和讨论；实际首次选择 `https://eleduck.com/categories/29`，READ UNKNOWN |

前三组供方噪声之后切换了社区，但仍没有直接打开具体买方需求。未得到原文，不能评价误筛/有效率，不能把本轮与上一批相加为独立项目数。原电鸭读取原始失败原因未记录，不能断言是验证码、404、解析或之前格式错误复发；也不能凭其他工具的过去超时替代本轮原因。

另做一次独立、只读的新URL诊断：来自本批搜索的 `https://www.aliyun.com/solution/tech-solution/ai-training-companion/2861523` 返回标题“构建AI英语口语陪练应用-技术解决方案-阿里云”与9,103字正文；真实 `PublicPageReader → effect_result(READ)` **ACCEPTED / 0.55秒**。没有模型/搜索重发、未走客户候选/判断链，也不是买方线索；只证实正常公开页与修复后的结果合同兼容，不能追认电鸭成功。

源码检查发现后续设计的硬约束：`open_web_reader_worker.read_request` 将非200响应（包含401/403/429）和网络异常都归为 `unavailable`；`PublicReadError` 不是可安全继续的充分依据。必须先分类确定的缺页/不支持内容与权限、限流、未知失败，再决定换源。当前没有为修复效果而放宽敏感字段、SSRF、登录或风控门禁。

原始脱敏证据：`/tmp/yike-education-live.JnJtdv/evidence.json`；新批次测试工具同目录，不提交凭据路径或完整公共正文到仓库。状态仍是实际完整研究失败，持续供给未证实；下一批需解决确定不可读结果的持久记录/安全继续，并保持UNKNOWN不可重放。
