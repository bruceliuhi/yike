# Mac 已部署服务候选验收（2026-09-11）

## 结论

**已交付可启动、内置真实 HTTPS 服务的 Mac arm64 验收候选；完整业务 UAT 未通过，不得作为正式商用发布。**

- 用户本轮授权：打包 Mac，测试已部署的 `https://yike.tuokexing.net`，修复并提交 main。
- 独立工作区 `/tmp/yike-mac-live-uat-20260911`，分支 `codex/mac-live-uat`。未更改并行主线程工作区、服务部署、数据库、账号授权或模型配置。
- 构包源码：`fa365e1b5a95f8cc76ac8cede46f7f59cc869c3f`，基于远端 `a7a9c4d`。后续主线变化不追认为本包功能。
- 服务部署记录中的运行 revision 为 `66745ef`；本轮未读取服务器运行态来重新证明 revision。

## 实际验证与修复

1. 本机公网 HTTPS 根路径返回303，TLS校验成功；`/readyz` 返回200，未禁用证书校验。
2. Node24.19.0 / npm11.17.0，锁定 `npm ci` 成功。服务配置/主进程请求定向9项通过，TypeScript通过。
3. 正式 `YIKE_RELEASE_SERVICE_URL=https://yike.tuokexing.net npm run make:mac` 成功。实际候选显示首页及账号设置，客户服务显示“已配置服务地址”；没有依赖运行时环境变量或任意服务重定向。
4. 实际客户端空手机号阻止提交。一次明确无效测试凭证经真实服务拒绝，仍未建立登录态。
5. 上述失败原显示“请先登录”，在登录页无法指导操作。修复仅限 `loginToken` 的401提示，保留原错误code/status及其它错误；普通session错误不变。
6. 先新增回归观察到1失败/16通过，再修复。最终登录、客户端传输、手机登录及发行配置4文件 **69 passed**，TypeScript及 `git diff --check` 通过。这里的单测替身不计为真实账号验收。
7. 非作者主线程针对精确提交 `fa365e1` 两文件差量审核 **GO**：错误码/状态和非401路径保留，无阻断。该结论不涵盖完整Mac UAT。
8. 修复后重新构包，实际启动并向真实服务提交同一无效测试输入，显示“访问凭证无效或已过期，请核对或向服务方获取新的短期凭证。”测试输入随后清空，留在凭证登录入口等待用户。
9. 原始Forge产物的codesign校验失败（既有签名资源不一致）。交付副本执行本地ad-hoc签名后 `codesign --verify --deep --strict` 通过，ASAR字节未改变。`spctl --assess --type execute` 仍拒绝，**没有Developer ID/公证，不是可信互联网分发包**；未关闭Gatekeeper或绕过系统警告。

## 交付产物

- App：`/Users/xingheimac/Developer/Work/yike-deliverables/mac-20260911/意客AI.app`
- ZIP：`/Users/xingheimac/Developer/Work/yike-deliverables/mac-20260911/YikeAI-0.2.0-mac-arm64-fa365e1.zip`
- ASAR SHA256：`7485a95f97c4fe335a14a371a1588664b8d774d73493c2fb62621cfecaaad9e9`
- 最终ZIP SHA256：`94a01e11280183106b259a652f697253671a6689c1292f56dce31304ff549436`
- 本地签名命令：`codesign --force --deep --sign - <交付App>`，然后使用ditto生成ZIP。此步骤只修复本地签名完整性，不授予Apple发行信任。
- 最终可见进程曾核验为PID57815、交付目录内的YikeAI；PID只适用于该次运行。
- 未将安装包、用户数据、诊断采样、密钥或凭证写入Git。

## 尚未解决与后续验收

| 项目 | 本轮结论 / 下一步 |
|---|---|
| 有效账号登录 | 未取得有效测试账号会话；已请求用户在客户端输入短期凭证，不在聊天传输秘密。登录后的画像保存、采集、商机、草稿/跟进均未完整实测 |
| Mac社交平台本地运行时 | `platformLoginConfiguration`拒绝非win32且正式bootstrap仅Windows。Mac尚未装配小红书/抖音/B站/知乎本地登录、采集、发送与回复；需真实跨平台运行时适配，不可简单取消保护条件 |
| 公开来源 | 有无需Python运行时的客户端实现，但本轮无已登录设备，未执行真实搜索或监控，不承诺可用 |
| 退出/重开 | 切包及菜单Quit后两次观察进程残留、CUA窗口读取超时。仅对本轮已核实PID57016/57602发TERM以继续验收，无平台任务/有效会话。根因未确认（包含一次运行中重构包干扰），不宣称正常退出通过；最终签名副本尚需单独重验 |
| 模型、短信 | 当前部署记录仍缺意客专用配置，本轮未调用或修改；不能验证AI推荐质量、短信到达 |
| 正式分发 | 需要Developer ID签名、公证及实际下载/安装验证；ad-hoc包不替代 |
| 协议/更新 | 原客户端仍有协议占位和未实现更新入口，未伪造正式文件或更新成功 |

没有对外发送消息、没有导入虚构商机充当真实结果。已完成的修复与构包不能替代上述真实验收，也不关闭完整V0.2目标。
