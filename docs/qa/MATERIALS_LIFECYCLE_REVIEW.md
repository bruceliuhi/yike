# Materials lifecycle independent batch review

Reviewed SHA: `85ddb164aab456368b20367d60cde46967a7ff74`; base `a266198`.
Verdict: **NO-GO at this SHA**. Review covers store, model/API/runtime and client assembly; no implementation edits by reviewer.

## Findings

- **P1 — Blocking authentication on the ASGI event loop.** `pilot/material_api.py:10-12,39-55` calls synchronous `identity(request)` directly from all four async routes. Identity performs PostgreSQL session authentication and waits for the session advisory transaction lock. A concurrent material parse holds that lock during its model call; a refresh or original-operation lookup consequently blocks the entire worker event loop, including unrelated customers. Offload the authentication path as well as service methods. Minimal isolated PostgreSQL/ASGI reproduction with a 0.8-second synthetic extraction delayed an unrelated 50 ms asyncio timer to **839 ms**; list returned 200 afterward.
- **P2 — Model wait holds the database transaction and session fence.** `pilot/materials.py:147-164` enters `_active` and an owner lock before extraction, and keeps both across the up-to-20-second provider operation. Same-session logout and authenticated reads queue behind extraction; the later `_active` cannot observe a logout waiting for its own held fence. This also contradicts AUTHORITY's short-transaction/no-model-wait constraint. Keep owner serialization/idempotency, but perform extraction outside the session/database transaction and reacquire/revalidate active session, original receipt and material CAS before committing.

## Evidence and boundary

Inspected current contract/plan, changed production source and focused tests; no additional substantive client DTO, original-receipt, quote-grounding, CAS, impact-token or owner-isolation blocker found in the defined batch. Reused implementer evidence supplied with the review: restricted-PostgreSQL store 6 passing; real HTTP/runtime/private child/loopback synthetic provider/PostgreSQL 2 passing; model/API/runtime 25 passing; client focused 48 passing plus fix deltas and TypeScript checking. These are supplied evidence, not rerun by this reviewer. Only the short concurrency reproduction above was newly executed; no full suite/build.

Empty managed-reference lists are honest for the currently implemented local profile-draft copy; managed-reference tracking is not claimed or added to this review. No real provider quality, customer UAT, platform execution, Windows package or production acceptance is granted. Re-review only these findings and their fix-related deltas at the replacement SHA.

## Delta review — `2de6e10267dd6b5b4ed10c7dc9abdb72d1b5e1c8`

Original P1/P2 are corrected: authentication runs in the thread pool; owner serialization uses a dedicated autocommit session lock, with separate preflight/commit transactions and a post-extraction active-session check. Supplied fix evidence: API 4 passing and PostgreSQL store 7 passing. No suite/build rerun.

**NO-GO: one introduced P2 recovery bug.** In `pilot/materials.py:182`, logout during the now-unlocked model wait makes the commit-phase `_active` fail before an operation receipt is stored. This correctly prevents material mutation, but leaves no terminal result for the original request. The existing client persists that request before dispatch and deliberately cannot clear it on a missing-operation 404 (`desktop/src/renderer/pages/profile/useMaterialRequest.ts`); after re-login, this profile remains blocked by an operation which can never complete. Minimal isolated PostgreSQL reproduction: pause extraction, revoke the original session, release extraction → `MaterialError`; issue a fresh session for the same owner and query the original request → `material_operation_not_found`, 404. Provide durable no-change recovery/proof without authorizing material writes from a revoked session; a generic missing receipt must not be interpreted as no change while work may still run.

## Final targeted delta — `7edfeb8301989f0deffdda866293a78fbf6572e0`

**GO for this materials lifecycle code batch.** The reported logout-recovery defect is corrected. After a previously authenticated/preflight-validated parse starts model work, a revoked/expired session causes the material transaction to roll back. While retaining owner serialization, the separate audit transaction verifies the original owner/tenant/profile and exact request digest, and persists only the bound `FAILED / confirmedNoChange` receipt. The original revoked caller still receives 401; a fresh active login can retrieve the terminal receipt and clear pending protection. Fresh requests using revoked credentials still fail before any receipt/material write. No new API, generic-404 no-change inference or revoked-session business mutation is authorized.

Reviewed only the recovery delta and its authority/rollback implications since `2de6e10`; no additional blocker found. Reused supplied RED→GREEN focused evidence: new logout recovery test passing and store file 8 passing (2.71 s), including original receipt, unchanged DRAFT/version, single model call and fresh revoked-request rejection. No additional test run or build by reviewer for this final delta. Earlier P1/P2 corrections remain accepted. GO is source-bound engineering acceptance only; all real-provider, platform, customer, Windows and production limitations above remain.
