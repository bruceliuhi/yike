# 04B 搜索建议：持久请求与 Windows 调用生命周期

日期：2026-09-09。承接模型 `e26c7a3`，基线包含 Mac 执行分工文档 `639b17d`。本记录只覆盖独立后台组件，不是默认服务入口、05C 客户端或整个 04B 完成；既有原文模型范围见[模型验收](V02-04B_SEARCH_MODEL_WIN_REVIEW.md)。

## 已提交的调用进程

代码 `a8707a81dfb8df1d3f14dfdf265c986b9df5e8ac`，只新增 process/worker/专项测试三文件，冻结模型适配器未改。

实际 Windows 子进程 + 本机合成 HTTP 验证正常原文建议、持续慢速流、stdin 写入前阻塞、DNS/传输关闭卡住、双运行关闭、启动与关闭竞争、未启动失败及清理异常。最多两在途（包括 STARTING），独立监督 I/O 与总截止；只有实际进程退出且 I/O 结束才成功。退出未确认保留句柄和槽位并拒绝新调用，不用 Future.cancel 冒充停止。

- TDD：初始 11 项缺模块 RED → 11 GREEN；新增管道关闭异常暴露的错误信息反例 RED → 修复；独立规格发现 STARTING 期间 close 后仍派发正文，新增反例 1 failed/0.27s → 同锁派发仲裁修复。
- 根代理最终 17 passed/0 skipped，10.95s；独立规格复审 17 passed/10.71s，PASS；独立代码/架构/质量 17 passed/10.75s，PASS，无本切片新增 P1/P2。
- 另一次模型＋进程组合 204 passed/11.06s 是关闭竞争修复前的验证历史，不冒充最终组合结果。最终进程 17 项已覆盖修复，模型 187 项源码未变。
- 无外部模型调用、客户资料、真实收费或数据库操作。子进程配置/正文走 stdin，不进入 argv/环境/落盘，非必要数据库/代理环境不继承。

冻结文件 SHA256：process `cb003957cd361af881f78ec174864c1fa5ac5835cfdca3d7019851844b2eec01`；worker `97d20481222a5384c8ff4ec42321a70756f6f26e42570ae26747f171639038d8`；test `f8e8eeda70af5aa91d3bfb3d6fda6f9984bf18fb1c5b5e230aef2094a668bcc2`。

## 已提交的请求持久层

代码 `32c70ddd684570a2f08869ca0d7649823d6d99b4`。纯 store/110/grant/两专项测试实现了原请求恢复、tenant/user 隔离、原会话完成绑定、画像摘要与逐字引用重验、终态回执及跨用户租户配额。独立规格初版 92 passed/0 skipped（35 纯＋57 真实受限 PostgreSQL 16），11.95s，脚本退出 0 且精确临时容器移除确认。

最终质量审核另以最小真实 PG 反例发现 P2：NOINHERIT 应用角色可 SET ROLE 到持有 TRUNCATE 的普通父角色，旧 grant 检查仍接受，配额表能被清空。实现者补直接/两跳成员角色的6个真实RED，修正仅限grant与PG反例。生产SQL保守检查MEMBER闭包、拒绝父角色超权，不修改其他角色权限；不引入PG16专有SET参数，也不宣称PG15已实测。

- 实现者最终98 passed/0 skipped（35纯＋63真实受限PG），13.34s；独立质量方再次运行15项grant定向通过、0跳过，并复核其余三文件摘要未变，五文件代码/架构/质量PASS，P2关闭。
- 自有脚本 `.runtime/search-suggestion-store-pg.ps1` 各最终验证退出0，精确随机容器ID核对及移除确认；没有使用客户库、扩大现有数据库权限或保留测试容器。
- 历史失败保留：初始缺模块/迁移RED；首轮71通过但Docker自动删除确认竞态使脚本退出1，改为有界确认后通过；PUBLIC列权限、异常claims/数据库异常链及上述NOINHERIT反例均先复现再修复，不回填成初版通过。

冻结SHA256：store `e67bd1ad257f44d4e91b8804dfba124de466f32bffef2563e09190b6435d39e3`；110 `d91a598aeacc59d1db96abbc13df35a26a321e6f9241e0168011dc83dfd92041`；grant `40e5de977387cf7b8ca6d248eb47f312c6fa8f055e7fa8a042b8371cc5659576`；PG测试 `80e1d2d39f03ca6dea085a2e1228bd60df15b736cb6c91a16d20e55300e90fc6`；纯请求测试 `1c4f67be568be9a2d0c0fccf5e8040a0cd8cdfce81750335eaf807e5b432cd62`。

## 本批次整合检查

在 `32c70dd`，根代理执行模型/进程/纯请求三文件组合：**239 passed/0 skipped，11.01s**。原始XML `.runtime/search-owned-7fc8700f3a104d25a6da3d6ea54f4dfe.xml`，SHA256 `310d2b94e58258ec631ff8652ea75182ee0ab4cd3b9e1409182bf927b3b8870c`。该239不含PG，不与98相加当全仓成绩。共享入口、冻结模型及其测试未改；差异和秘密扫描通过，没有重跑无关全仓、桌面打包或生产验收。

## 未完成与下一步

- 有序关闭的总截止默认30秒，另有共享3秒清理宽限；close 默认共享5秒等待预算。OS进程创建卡住不能报关闭成功。父进程硬崩溃后的孤儿清理、Linux/Mac实际进程验证未完成；关闭本地连接不证明供应商取消处理或费用。
- 后台四任务准入/调度、认证 HTTP、外发授权快照、旧页面持久请求恢复及真实模型效果仍需接通。原画像确认不等于自由文本允许外发；`openai-compatible` 是协议类别，不是实际接收方名称。
- 110 未注册共享 `pilot/db.py`；Mac 保留共享入口串行整合及111执行迁移，Win继续108/110、04A/B和05B/C。建议回执不是确认策略或执行许可。
- 最终策略版本/预算及同事务 resolver、真实“多找类似”、原文发现/判断/联系/跟进整链继续；测试组件可用不代表首个客户试用版本已完成。
