# Windows HTTPS 候选验收（进行中）

目标：绑定已部署的 `https://yike.tuokexing.net`，生成同源离线payload和Windows包，按真实用户路径验收。当前不是发布通过记录。

## 构包阻断修复

基线 `a7a9c4d`。第一次生成payload因旧运行时治理摘要落后被拒绝，旧目录保留；随后按当前锁全新安装，在SOURCE阶段真实复现`patched_file_set_mismatch`，尚未安装依赖或发布成功回执。

根因：治理清单包含三个与上游相同但需要固定摘要的知乎依赖（`media_platform/zhihu/help.py`、`model/m_zhihu.py`、`store/zhihu/_store_impl.py`）；它们的实际摘要均与锁一致，但不在Git修改集合内。安装器错误要求两集合完全相等。改为拒绝清单外修改，并继续逐一核对所有锁定文件摘要；不修改治理锁、平台权限或运行时版本，不采用旧目录或伪造成功回执。

回归先RED（2 failed / 16 passed），修复后18 passed，包含合法未变文件、篡改摘要、清单外修改及原安装失败边界。非作者独立代码/架构/测试审核GO，绑定本批两文件摘要；真实新目录安装验证及最终包/可见UI证据后续追加。本改动不影响服务器Dockerfile输入，不重新部署服务镜像。

## 尚未完成

最终payload、Setup及ASAR摘要、安装/首次准备/退出重开/单实例/卸载、真实HTTPS登录和完整用户业务操作尚未取得本轮通过证据；历史包不替代当前包。短信及模型配置、平台账号验证、批准后的真实发送和回复、客户试用与发行签名仍单独待验，Goal保持进行中。
