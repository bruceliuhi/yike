# 主干整合独立架构审核

- 审核日期：2026-09-09
- 候选提交：`cd642bddfc1238853a21a7aef39a2c0432dbe38a`
- 最新主干比较基线：`e7c94a9`；候选父提交：`6a70dd9b384681ddfa7fd4a17e68efcf33d7278e`
- 独立审核者：`design_handoff_architecture`
- 结论：**PASS — 本审核范围内未发现新增 P0/P1，可作为本轮 main 代码整合候选。**

本结论是源码、合并关系及已有证据的独立审核，不是完整付费版、生产部署或 Windows 实机交付批准。审核时 HEAD 为上述完整 SHA，工作树干净；本文件随后加入，不改变已审实现。

## 独立性与范围

审核其他成员编写的 `pilot/store.py`、`pilot/research_import.py`、`tests/test_import_atomicity.py`；Windows 构建证据脚本、PowerShell 入口、验收模板及说明；根代理处理的主干治理与历史范围冲突。补充检查隔离视觉 harness 的入口、网络/存储隔离和生产排除记录。

**排除本人实现的 `scripts/package_mediacrawler.sh`、对应打包测试和包装说明，不给这些修改签发自审结论。** 这些内容交由独立代码审核者复核。此前本人实现的 UI facade、画像、候选等页面未在本轮改动，不以本次审核重新批准它们。

## 核验结果

1. **整包事务与来源身份**：受信研究包先完成输入验证，再由 `PilotStore.import_opportunities` 在同一个 `psycopg` 连接事务内写入所有条目。后续条目失败时异常退出事务，包含来源、来源版本、观察及商机的先前写入一起回滚。单条入口复用同一内部方法；未把外部网络、浏览器或模型等待移入事务。相同导入键在返回幂等结果前校验画像、平台/外部 ID 和公开 URL，不能用重复键掩盖 URL 冲突。
2. **租户和现有安全边界**：保留当前 `provision_tenant` 的租户配置修复。导入、来源联结及最新跟进子查询保留服务端解析的 tenant_id 与 SQL 租户谓词，画像确认状态和既有锁仍在。相较比较基线，`pilot/db.py`、UI facade、Web 认证、迁移、`desktop/src/` 和 Forge 配置没有修改；没有新增客户端管理员入口或放宽 Origin/RLS。新增 PostgreSQL 回归要求不同管理员/应用角色、同一隔离数据库，且应用角色不得为超级用户或 BYPASSRLS。
3. **Windows 证据语义**：九个自动阶段分别保存真实退出状态，失败不会把后续阶段记为通过；未完成运行不能得到 `BUILD_SUCCEEDED`。实际产物记录大小与 SHA-256，限制为 `out/` 内的真实文件并拒绝符号链接逃逸。报告不采集环境变量、原始日志或异常全文。自动成功仍保持人工验收 `UNTESTED`，安装、可见启动、重启、退出、卸载及显示缩放必须另填实测结果。PowerShell 的 Node 缺失/版本错误兜底也保留失败语义，不能把 macOS 脚本检查当成 Windows 运行证明。
4. **治理与合并**：保留 `554c1ed`、`e7c94a9` 的并行主线历史，并在旧任务板/计划入口明确其 DISCOVERY/SQLite 范围不是当前授权。根权威和任务书继续要求 V0.2 多平台、PostgreSQL、R3 客户端、确认后真实触达和回复；整包事务仅改善受信研究包导入，不被写成原始候选上传或平台已接通。旧演示的 Jinja 界面、启动器及四页范围没有覆盖 R3。产品代码归 `yike-ai2026/main`，后续从最新 main 建 `codex/` 分支，正常合并；`yike-ai` 只读、暂不迁移或归档。
5. **视觉验证隔离**：独立 Vite 入口固定到 `127.0.0.1:18794`，harness 使用内存存储、TEST 标记与阻断的业务网络/原生桥。生产排除脚本检查真实 renderer 构建图、manifest 和现有 ASAR；记录明确仅已操作部分页面，未完成全部 20 页同状态截图对照，未生成新客户端包。该 harness 不能作为真实业务成功证据。

## 证据及读取方式

- 本审核者直接读取候选源码与 `e7c94a9..cd642bdd` 差异，核对保留的 RLS 行、未变化的认证/数据库迁移/客户端源文件和合并祖先关系；未重复执行完整业务测试。
- 已读取 `docs/qa/ui-r3/windows-evidence-writer-check.json` 及 `desktop/tests/visual/VALIDATION.json`。前者记录 macOS 上 7 项证据写入测试、非 Windows 入口真实拒绝及其余阶段 NOT_RUN；后者记录 15 项隔离测试与生产排除检查，均明确其环境和局限。
- 已读取绑定候选的 [checks.json](checks.json) 与[整合验收记录](REVIEW.md)：根代理执行桌面 typecheck 与 **27 文件、238 passed**；隔离 PostgreSQL 16 的不同管理员/非超级用户且无 BYPASSRLS 应用角色下，导入、UI API、试用页、租户契约及 vendor 定向套件合计 **95 passed、0 skipped**；秘密扫描 clean、Shell/编译/差异检查通过。checks.json 明确是本轮工具输出的精简转录，并保留命令、退出码与工具输出标识，不能当成原始日志。此处不冒称本审核者独立重跑这些测试。
- 打包器的独立审查和测试由其他审核者签发，不计为本文件对本人代码的批准。

## 保留限制

- Windows PowerShell、真正的 x64 安装包构建及人工安装/运行/卸载/缩放尚待用户在 Windows 上执行和回传；自动报告只能记录其实际完成范围。
- 本轮未重新构建 macOS 候选包；此前包的绑定结论仍仅适用于其原提交和文件摘要。
- 新增 `latest_followup_status` 是后端查询字段；现有桌面 `mapOpportunity` 尚未使用它，不能宣称“已回复”等细分跟进状态已在 P10 列表和 CSV 全部贯通。这不阻断该后端增量入主干，须在后续前端任务中完成接线及验收。
- 多平台真实连接、采集执行、候选上传、正常客户登录、发送/回复服务、剩余页面状态和视觉验收继续按任务书推进；没有用本轮测试覆盖这些尚未完成的能力。
- CP-06 的生产服务器、HTTPS、数据库 ACL、备份恢复、回滚及真实手机证据仍保留，未以本地 PostgreSQL 回归代替。
