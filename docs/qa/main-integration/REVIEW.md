# 主线整合验收

日期：2026-09-09。代码候选：`cd642bddfc1238853a21a7aef39a2c0432dbe38a`。文档整合候选：`386e8e28c0c9360af168d536c5dd86545535e0e9`（包含 `238172f` 并行文档合并）。合并目标：`xinghetech/yike-ai2026:main`。

## 本轮变化

- R3 前端及 macOS 候选 `615e340` 已快进到 `main`，远端默认分支已核实为 `main`。
- 合并 `4d43244` 采集组件打包分支，保留当前 V0.2/PG/R3 约定；修复伴随清单覆盖、源 Git 引用破坏和历史 `.env*` 漏检。只验证了本地 fixture 打包，未分发真实上游 bundle、未运行平台采集。
- 从 `afc7e8b` 精确移植受信研究包整包事务、同键来源 URL 冲突校验及商机查询源/跟进字段；保留当前 RLS/provisioning 和 UI facade。未搬旧 Jinja 页面、演示启动器或旧四页产品约束。
- Windows 构建脚本增加源码、系统/Node、阶段退出码、锁文件/安装产物哈希及失败报告；人工安装和缩放记录默认 `UNTESTED`。
- 增加独立 `tests/visual` 入口，复用真实页面并使用明确标记的内存 TEST 状态；禁止真实请求、发送、下载和客户库写入。生产 renderer 与现有 ASAR 排除校验通过，20 页完整同状态视觉比较仍待继续。
- 保留并行 `main` 提交 `554c1ed`/`e7c94a9` 及 `6f449e6` 的 V0.2 范围修正。协作建议服从唯一实施台账，不重置当前 Mac 前端 Goal、`pilot/desktop` 入口或用户手动 Windows 验收安排。

## 验证

当前候选的源代码与以下执行时的工作树一致；随后只提交代码及补充验收文档。命令、结果与工具输出来源见 [checks.json](checks.json)。

| 检查 | 本轮结果 |
|---|---|
| Node 24 桌面类型检查与完整桌面测试 | 类型检查通过；27 文件、238 passed |
| 导入、UI API、试用页、租户契约与源码打包回归 | 隔离 PostgreSQL 环境，95 passed，0 skipped |
| 新增整包导入回归 | 包含在上行；5 项均以独立管理员和非超级用户、无 RLS bypass 的应用连接运行 |
| 数据写入失败路径 | 后续冲突回滚商机、来源、版本和观察；同包/同键 URL 冲突拒绝；重复导入幂等；跨租户拒绝 |
| Secret scan、Shell 语法、Python 编译、差异检查 | 通过 |
| Windows 证据 writer | macOS 隔离 7 项回归通过；实际 CLI 正确拒绝非 Windows 宿主并保留失败报告 |
| 视觉工具隔离 | 15 项回归、生产模块图/manifest 与既有 Mac ASAR 排除检查通过 |

Windows writer 原始输入摘要与本机失败报告摘录见 [writer 检查](../ui-r3/windows-evidence-writer-check.json)；视觉工具细项见 [隔离验证](../../../desktop/tests/visual/VALIDATION.json)。上述用例只能证明对应工程行为，不能替代真实平台或客户业务验收。

## 独立审核

- [架构审核](ARCHITECTURE_REVIEW.md)：排除审核者编写的打包器修复。
- [代码审核](CODE_REVIEW.md)：排除审核者编写的 Windows 证据工具。
- [质量审核](QUALITY_REVIEW.md)：排除审核者编写的视觉工具与后端移植。

三份记录均绑定代码候选 `cd642bdd`，结论限定为本轮代码整合无新增 P0/P1；代码审核另复核并行文档增量。两个候选的产品源码一致，没有因文档更新重复运行全套测试。上述审核不批准完整付费版、Windows 实机或生产上线。

## 交付边界

本轮为代码主线整合，不是生产发布。既有 Mac 客户端运行源码、资源、打包配置和依赖锁与 `10ab8b6` 相同，本轮未重新构建 Mac 包。Windows PowerShell、安装/启动/退出/重启/卸载及 100/125/150% 缩放仍由用户在 Windows 上执行；目前不能宣称通过。

前端 Goal 继续进行：仍有部分条件服务交互、同状态视觉对照及原生导出保存回执需要完成；新增后端跟进状态字段尚未全部接入桌面显示。实际登录、采集、监控、收发服务与生产环境仍须分别验收。`yike-ai` 本轮保持只读，没有迁移治理文档或归档仓库。
