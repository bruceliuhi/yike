# 独立限定包与证据复核

2026-09-10，复核对象为 `0d17cf708e371fd61e502d168b8bd67d308f69e1` 的本次 Mac 包、记录及指定截图。只读产品和产物，未重新构建、运行浏览器或提交代码；未接触四个未跟踪 rawCandidate 草稿。

结论：本次限定交付未发现新的 P0/P1 阻断。来源、实际包字节和记录一致；不能据此签收全部 UI Goal、真实服务业务闭环或 Windows 安装验收。

## 独立核对

- 逐项从 Git 提交重新读取 365 个 desktop 文件，并与 `final-build-inputs.json` 的 SHA-256、隔离构建目录文件逐字节摘要比较。Git 树和清单均为 365 项，无遗漏、额外项或摘要不一致。
- 重新计算交付 App 的 `app.asar`、交付 ZIP 及 ZIP 内唯一 ASAR。ASAR 均为 `750ea0f759c10a6786ba2544e05c01f49af945211d4622854f59e3c75901698e`，ZIP 为 `d34f571e9c91054a705775863d8cb4ac14d4f9bcb50a8c0c027466ae9f260dd3`，与包清单一致。
- 直接读取实际 ASAR：版本 `0.2.0`、入口 `.vite/build/main.cjs`；重新计算结构记录列出的 38 项 renderer 文件摘要，全部一致。
- 检查构建/结构/类型检查、严格包 smoke、生产隔离排除日志及其调用脚本。make 完成 arm64 ZIP；smoke 有完整 PASS 行，其脚本要求稳定工作台、桥接约束、临时真实文件导出与取消、退出确认，以及正常 `will-quit` 的完成标记，不能以早退充当成功。生产排除结果为 `existingAsarChecked=true`、`failures=[]`。本复核未重跑这些命令，类型检查空日志本身不单独充当通过证据，结合串行构建脚本成功后才写出的输入清单核对。
- 读取 AX 06–09：实际来源为 `yike://app/index.html`；P07 未登录展示登录入口；公开样例的客户画像、排除与入库均禁用且有解释；重启记录回到工作台。独立查询 PID 29140，当时仍为交付目录下 `意客AI.app/Contents/MacOS/YikeAI`，实际 ASAR 摘要与 restart identity 一致。退出前后操作顺序依赖执行者记录，本复核未重复操作客户端，也未把 AX 文本当作真实客户写入证明。

## 实际看图与范围

已通过本地图片查看工具打开 `03-confirm-960.png` 和 `07-native-candidates-signed-out.jpg`。960×600 确认弹窗的标题、版本及核验 ID、五项依据、复核勾选和底部按钮完整可见，无该截图范围内的裁切；未登录原生页导航、筛选与登录入口正常显示。前者显式 TEST 标识，不能当作原生已连接客户服务；后者不能替代有数据的表单链。

也已打开原 `02-confirm-1440.png`（现更名 `02-confirm-1440-rejected-compositor.png`）：右侧存在重复绘制，截图无效，不计入 1440 同视口视觉 PASS。不能仅凭该采集异常判定产品有双导航。README 已明确作废并保留其他分辨率待验，因此这一证据质量问题已正确收敛，不构成未披露的 P2 产品问题。

README 已正确区分冻结 TEST 表单回执、实际原生未登录/样例页面、包内隔离 smoke 和真实服务；未发现需要新增修复的 P2 包或证据描述问题。完整设计配对、其它页面状态、原生恢复文件链、客户服务接入和 Windows 安装/启动/退出/重启/卸载不在此次签收范围。
