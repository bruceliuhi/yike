# UI 流程补齐：质量与视觉复核

审核人：Agent B。日期：2026-09-09。

当前结论：**本报告独立代码审核范围未发现剩余 P0/P1；桌面截图可读且主要操作无遮挡，但仍有下列视觉差异，不能宣称全部页面与设计同状态、逐像素一致。** 流程实现候选绑定 `237a5b2a6be4f68682fcae4312f338e2338a3646`，基线为 `30da93e5ba39c9bc11227640b06094c7afe980b2`。后续平台品牌图标为独立增量，不能直接沿用该提交的 487 项结果。

## 独立代码范围

- A 的 P03/P04：`domain/materials.ts`、`services/materials.ts`、`pages/profile/`、`Profile.tsx`、资料测试和[资料契约](../../UI_MATERIALS_CONTRACT.md)。检查异步等待、用户/画像版本隔离、持久原请求保护、回执身份、人工内容应用及现有画像 facade 兼容。
- 主 Agent 的 `TodoQueue`、`Workbench`、候选精确定位、`contactList`、`ContactNotes` 及共享 `operationLedger` 本轮新增 scope/格式。未将既有 ledger 实现重新算作独立审核。
- **排除自作代码批准：** B 的 P14/P15 跟进模块、历史 P12/P13、hooks/Modal/ledger 既有实现；跟进模块由 [A 的架构审核](ARCHITECTURE_REVIEW.md)交叉覆盖。下文仍按主任务要求实际查看 P12–P15 的运行截图，但这不构成对自作业务实现的独立代码批准。

资料模块只在可选服务存在、已登录且选中已保存画像版本时进入客户空间路径；缺服务仍明确为本机资料草稿。资料的保存、解析、提取确认、撤销/移除和原请求查询相互区分。核对中画像内容变化不会自动覆盖人工字段；填入画像草稿也不会自动保存或确认画像。资料、跟进、任务等可选服务在生产是否接通，不能从测试适配器推定。

本次发现的资料 P2 已修复：成功回执原先只检查对象和版本，现在即时保存还逐项核对正文、名称、用途、引用范围和文件元数据；提取确认核对 extractionId 及人工选定字段。错配保留输入与原请求锁。原请求查询不保存正文，服务端必须按 requestId 返回原操作结果，不能拿并发产生的最新版本冒充该回执。

工作台旧兼容列表原先可能混入样例，现已先以 `isSample` 排除，再计算空态和截取列表。精确候选入口按 ID 请求，拒绝其他 ID；目标变化会清除旧选择与确认面板，读取有超时，目标缺失给独立空态。备注保持用户/商机/画像版本的本机会话范围，不进入消息提交内容。共享 ledger 新格式不放宽已有发送和管理操作枚举，不能替代服务端持久幂等和权限检查。

## 视觉方法与边界

逐页使用 `view_image` 在同一次工具输入中先打开 `design/v02-suite-r3/screens/Pxx.png`，再打开本轮 `screens/Pxx.png`，实际检查下列 **15 对图**：P01/P02/P03/P05/P07/P09/P10/P11/P12/P13/P14/P15/P16/P17/P18。不是依据文件存在判定通过。

运行截图为主任务捕获的隔离视觉 harness，CSS 视口 **1484×1060**；本审核人未操作浏览器。图中顶部 TEST 标识、TEST 配置和内存状态不是客户事实，不证明真实登录、平台采集、发送、到账或客户数据库写入。P05/P07 参考图的 1px 尺寸差不计为布局缺陷。浏览器状态元数据见 [reference-states.json](screens/reference-states.json) 和后续覆盖的 [refined-states.json](screens/refined-states.json)；同一页面以后者及实际截图为准，不能沿用早期错误 query 的状态说明。

字号按照 [R3 DESIGN_SYSTEM](../../../design/v02-suite-r3/DESIGN_SYSTEM.md) 的标题 24/32、区块 16/24、正文 14/22、辅助 12–13/18，以及统一侧栏/按钮 token 评估。参考栅格中的大字和生成边缘不作为 CSS 测量尺，也不为了贴图放大整页。截图中系统中文可辨、标题/字段标签有层级，固定导航、Y 标志和蓝色主动作一致；没有新增装饰插画或大阴影卡片。

## 逐页观察

| 页 | 实际观察与状态差异 |
|---|---|
| [P01](screens/P01.png) 登录 | 双栏品牌/表单和中间分隔保留，手机号、验证码、登录、试用码和协议入口均可见。字体按 token 比原栅格紧凑；增加的已有访问凭证入口为真实兼容路径。没有凭此空表单宣称短信服务完成。 |
| [P02](screens/P02.png) 工作台 | 三步引导、四待办页签、右侧使用路径、独立公开样例完整可见。当前为空待办，非满队列视觉验收；样例说明和查看入口未与客户待办混放。 |
| [P03](screens/P03.png) 画像 | 双栏字段、版本选择、摘要、底栏均完整。参考的“AI 整理后画像”改为基于当前内容的确认摘要，避免冒称 AI 已执行；这属于状态语义差异。摘要使用一层浅底容器，没有卡片套卡片。 |
| [P05](screens/P05.png) 采集任务 | 搜索/平台/日期/原始线索入口和本机草稿行可读，未启动标识明确。参考的“采集任务/原始线索”页签和字段表改成状态页签与列表，不能称版式完全还原。再次实际看图确认“保存的模板”空段已展示，分页数量与按钮已同行。模板明确为本机会话、未同步。 |
| [P07](screens/P07.png) 原始线索 | 左列表与右证据复核保持分栏，来源/发布时间/地点/截止时间/原文/画像条件可见。实际选中公开样例，排除和入库保持禁用；增加独立客户/样例页签。不是客户入库成功状态。 |
| [P09](screens/P09.png) 监控详情 | 五平台状态与右侧选中平台原因、账号、重新连接可见。当前为 TEST 部分完成组合，顶部显示重试/取消，与参考状态的暂停动作不同。当前内容高 1431px，首屏不包含执行统计，必须滚动；本图不证明整页一屏显示。 |
| [P10](screens/P10.png) 商机库 | 筛选、公开样例行、证据入口及禁用导出完整可见，无横向遮挡。阶段/截止字段差异见下表；实际只有 1 条样例，不验证客户大列表密度。 |
| [P11](screens/P11.png) 商机详情 | 原文、需求概述、匹配理由、行动信号、价值与风险、联系准备及复制动作都可见。预算询价仍标公开研究样例/未入客户库，没有包装成订单。 |
| [P12](screens/P12.png) 联系准备 | 三栏布局、用途切换、收件对象、账号、正文、证据和备注齐全。样例只读，当前唯一可用蓝色主动作是复制样例；保存/发送禁用，与规范勘误一致。84 字为当前完整正文计数，不能照抄参考图 82。 |
| [P13](screens/P13.png) 确认发送 | 模态框完整位于视口内，正文、复选框、样例不可发送说明和底栏没有重叠。对象/渠道按最终只读快照呈现，与参考图可编辑样式有意区别；焦点蓝边不是多余装饰。 |
| [P14](screens/P14.png) 跟进 | 五列表头、负责人/日期、添加入口、右侧回复页签与空态可见。未选记录时为“暂无未匹配回复”，不假称当前客户从未回复。比参考减少大外框和空白容器；此图不代表有记录/错误等全部状态。 |
| [P15](screens/P15.png) 添加跟进 | 标准视口为无蒙层右侧面板，左侧列表随之收窄。名称、四种事实类型、时间、备注、下一步、日期、负责人和保存底栏可见；取消在左、主动作在右。首屏末段需滚动，再次实际查看 [1484×1060 滚动图](screens/P15-scrolled.png) 和 [960×600 滚动图](screens/P15-960x600-scrolled.png)，两者均完整显示末段说明、负责人和保存底栏，未相互遮盖。属于浏览器证据，不是 Windows 实机证据。 |
| [P16](screens/P16.png) 平台连接 | 五平台表格、未连接/无需账号、能力边界和连接入口完整。最新图已统一为与 P09 相同的 Globe 图标，原视觉不一致已关闭；没有已连接账号或伪造能力数据。 |
| [P17](screens/P17.png) 平台登录 | 三步、当前等待状态、检查按钮和打开登录窗口均可见，底栏不换行、不遮挡。当前是等待打开窗口，较参考“等待登录”更早，不能据此宣称账号授权成功。 |
| [P18](screens/P18.png) 授权设置 | 使用授权、设备、数据与支持各段分隔清楚，右侧操作对齐。增加实际环境、客户服务、清草稿和退出入口；最后按钮仍可见。元数据内容高 1079px，比视口多 19px，不能宣称完全无纵向滚动。 |

P04/P08/P19/P20 不在本审核人的逐图批准范围，由主任务分别核查；P06 的样板前后对照也由主任务核查。初轮 P19 内容高 1120px，之后主任务调整表格基础行高和摘要间距，并报告在 1484×1060 下已无文档纵向溢出。当前 P19 结论应以后续 [design-qa](design-qa.md) 的最终截图和测量为准；本审核人没有独立操作浏览器复测，不能把旧尺寸或主任务报告冒充本人的最新实测。

## 视觉差异与剩余改进

| 优先级 | 位置 | 具体问题与最小处理 |
|---|---|---|
| P2，已修 | P05 分页 | 原先“每页 / 10 / 项”分成三行。再次实际查看最新 P05，已与左右分页按钮保持同行；模板空段也已补齐。 |
| P2 | P10 列表 | 参考图有独立阶段和资料截止；当前“阶段/状态”列只显示状态，“资料截止”换成更新时间。当前公开样例的截止与阶段可在 P11 查看，但列表扫读信息没有按参考保留。现有 Opportunity 契约没有独立 stage/deadline 字段，补列需明确结构化数据来源；没有字段时使用“—”，不得从样例推填所有客户或由文字猜测事实。 |
| P2，已修 | P09/P16 平台图标 | 原先监控表使用 Globe、连接表使用 ChatCircle。流程候选 P16 已统一 Globe，经再次逐对看图确认。用户此后要求的官方平台品牌图标属于下一独立增量，应另验渲染与来源。 |
| 已核对，无剩余遮挡 | P15 右侧面板 | 首屏末段需滚动，不代表内容不可达。实际看过两张滚动图，说明完整可读；主任务 [测量](screens/P15-scroll.json) 记录标准视口 body 高 858、内容高 916、scrollTop 57.6。没有把主任务的浏览器操作冒充本人的测量，也不宣称长表单一屏展示。 |

P05 的列表结构、P09 的较长页面仍应作为设计与实现差异保留说明；不得用“截图已生成”替代功能完成认定。上述判断没有把更小但符合 token 的正文、真实字段变化、样例只读限制或系统日期控件当成错误。

## 实际命令证据

以下由本审核人实际执行，工作目录 `desktop`，Node 24。工具 chunk 是会话输出检索标识，不是仓库中的原始日志文件。

| 命令 | 结果 | 输出 |
|---|---|---|
| `./node_modules/.bin/vitest run tests/ui/profile-materials.test.tsx tests/ui/profile-materials-integration.test.tsx tests/ui/profile.test.tsx tests/ui/contact-preparation.test.tsx tests/ui/workbench-queue.test.tsx tests/ui/operation-ledger.test.tsx` | 6 文件，56 passed；材料回执修复后 | `324c7b` |
| `./node_modules/.bin/vitest run tests/ui/workbench-queue.test.tsx` | 1 文件，13 passed；工作台过滤/精确目标修复后 | `59a137` |
| `npm run typecheck`、`git diff --check` | 通过 | `3f8a52` |

两次测试包含重叠文件，不能相加为 69 个独立测试。本报告未重跑完整仓库/真实 PG 测试，未验证生产 MaterialService、WorkbenchService、FollowupService、TaskOperationsService，未执行真实采集/发送、安装包打包、Mac 安装或 Windows 实机验收，也不替代 CP-06。

### 后续 TEST 跟进适配

主任务为可操作的视觉检查追加 `tests/visual/followup.ts`。本审核人按授权实现这一测试适配器：固定 TEST 商机、画像和成员，1 条合成人工登记与 1 条明确标识的合成回复；创建/纠正/撤销/标已读和原请求查询只作用于实例内存。重复 requestId 返回原回执，内容变化拒绝执行；未观察到的请求仍为 UNKNOWN。它不发送消息、不访问网络、文件、持久存储或客户数据库，只有测试入口显式接线后才使用。

`./node_modules/.bin/vitest run tests/visual/followup.test.ts` 实际 **1 文件、4 passed**，`npm run typecheck` 通过（`ebe420`）。这是自作测试工具的开发回归，不能列为独立产品代码批准，也不能证明生产跟进服务已接通。浏览器流程与其截图由主任务另行记录。

### 冻结后的完整前端回归

2026-09-09 **18:40:17（Asia/Shanghai）**，主任务声明生产源代码冻结、最后 P19 CSS 收敛后，本审核人在 `desktop` 使用 Node 24 实际运行：

```sh
npm test -- --run
npm run typecheck
```

结果：**49 个测试文件全部通过，487 passed；typecheck 通过**。Vitest 4.1.11，测试耗时 8.71 秒，工具输出 `ba6754`。该总数包括现有及本轮新增前端/契约/隔离视觉测试，已包含上述定向测试和 4 项 TEST 跟进适配测试，不能再次相加。

这次完整执行补充了前述定向代码审核证据，但不扩大其独立批准范围：测试中包含本审核人自作模块，由其他 Agent 交叉审代码。没有执行新的真实数据库、平台通道、安装包或 Windows 验收。README 已同步 `reference=r3` / `capabilities=complete` 的纯 TEST 语义；两者均不启用生产服务。此后任何源码或 CSS 修改应另做适当增量验证并记录，不能直接沿用为修改后的结果。

## 最终绑定

流程候选为 `237a5b2a6be4f68682fcae4312f338e2338a3646`，487 项完整回归绑定这一批流程实现，独立代码范围与自作模块排除项保持不变。P05/P15 收敛图已经再次实际查看，旧视觉问题已按上文关闭；P10 结构化字段差异仍保留。可以作为前端候选代码和上述视口视觉范围的审核依据，不能单独宣称产品已上线或所有设计状态完成。

### 平台品牌图标增量的定向回归

本审核人随后仅在 `Opportunities.tsx`、`Outreach.tsx`、`followups/RelatedReplies.tsx` 接入共享 `PlatformIcon` / `PlatformLabel`。来源平台与机构、客户名分开呈现，native option 和 CSV 内容保持原逻辑；未改采集、发送或写入权限。本段属于自作视觉增量回归，不构成对该增量的独立代码批准。

2026-09-09 **18:49:58（Asia/Shanghai）**，在 `desktop` 运行：

```sh
./node_modules/.bin/vitest run tests/ui/opportunities.test.tsx tests/ui/opportunity-export.test.tsx tests/ui/candidates.test.tsx tests/ui/outreach.test.tsx tests/ui/contact-preparation.test.tsx tests/ui/outreach-queues.test.tsx tests/ui/outreach-reconciliation.test.tsx tests/ui/send-confirmation.test.tsx tests/ui/followup-completion.test.tsx tests/ui/followup-routing.test.tsx tests/ui/followups.test.tsx
npm run typecheck
```

结果 **11 文件，141 passed；typecheck 通过**（工具输出 `9d7c5f`）。这些是既有套件的增量重跑，不能与 487 相加；全平台品牌组件及最终截图由主任务另行交叉核对和绑定。

### 品牌截图增量复核

品牌源码绑定 **`1c1567325bca971a723c87983efb0c289c57e03a`**。本审核人逐对在同一次 `view_image` 工具输入中打开 R3 原图与 `screens/brand/` 下 P06/P09/P16/P17，又逐对查看 P11/P12 的来源标签；另外查看 P11/P12 原始截图以区分归一化影响。本段未重新测代码，也未操作浏览器。P11/P12 属于本人修改的页面，本段只是图像观察，不是独立代码批准。

| 页 | 品牌与来源观察 |
|---|---|
| P06 | 四个平台品牌及公开网站 Globe 在右侧状态表和下方平台选项中使用一致，图标与文字同排，没有顶开关键词或覆盖复选框。B站采用此次提供的蓝色电视标，与生成参考的粉色标有资产版本差异，不按参考栅格强行重绘。待连接和范围待确认仍可读。 |
| P09 | 平台行品牌、选中小红书详情图标和名称对应，状态徽标、账号及重新连接动作未混入品牌含义。未增加新增量数字；TEST 部分完成与参考状态不完全相同，长页统计仍需滚动。 |
| P16 | 五行图标、平台名、账号、状态和连接按钮维持清晰列对齐。公开网站为 Globe 且明确无需账号；四个品牌并未被当作账号已连接证明。 |
| P17 | 小红书图标显示于登录状态块，当前“等待打开登录窗口”与原参考“等待登录”的差异保留。模态框内步骤、说明、取消/检查/打开按钮均有独立空间，没有品牌遮挡或新增假二维码。 |
| P11 | 当前为 TEST 客户记录，来源平台与证据来源均为公开网站，Globe 符合数据；不应套用其他平台品牌。来源图标与“公开网站”整体同排，未把 TEST 需求方当作平台。 |
| P12 | TEST 客户行的平台图标、独立公开样例的机构来源和右侧证据来源各归其位。Globe 是预期，机构名称仍保留；没有把买方文字变成平台名。 |

**截图证据待收敛：** 初次品牌截图 P06/P09/P16/P17 在约 y=978 以后重复出现整条 Logo、面包屑、TEST 与 V0.2 顶栏，P06 底部保存/下一步仅露上缘；P11/P12 则全图明显模糊，查看 raw 仍如此。已报告主任务核对捕获与真实页面，尚不能仅据这批图片断定是产品 DOM 问题，也不能将其批准为完整无遮挡、字形清晰的最终证据。品牌资源与主要标签位置未见具体阻断，截图问题须以重拍或实际页面核验补充。这里没有审核品牌版 P08，折叠态与前轮展开态的差别不记为回归；小窗口品牌截图由主任务负责。
