# 本机 UI Goal 交互收口记录

后续接入说明（2026-09-10）：下文“未提交/未独立审核”描述原本地候选。源码已由9999355/e70c3a1进入a31069f，独立接入审查后的3898c3e修正及两项发现关闭见[组合验收](../V02_CONFIRMED_STRATEGY_COMPOSITION.md)。该结论是限定代码接收；历史构包、真实交互、Windows及完整UI Goal的未完成项继续保留，不把旧测试或旧包重新绑定到新提交。

更新：2026-09-10。**当前为未提交的本地候选，完整 Goal 仍在进行。** 开始基线为 `929196322c626516e1fc88c5d687a89cde3c334e`。本轮保留原 R3/R4 授权与历史证据，不将下列增量当作全页面状态验收或正式发布。

## 本轮实现

1. **关闭前保护会话草稿**：已保存到本机会话、且编辑器已离开的任务也会触发原生退出提醒；内部页面导航不会因此被阻拦。清除草稿后的墓碑、拒绝存储时的内存副本和 StrictMode 清理均有回归。当前仍是会话存储，明确放弃并关闭会清除，不宣称跨重启持久化。
2. **P09 任务画像版本对照**：只在服务返回稳定业务实体关联时比较绑定版本与当前确认版本；不按名字或全局最大版本猜测。缺关联、重复确认、响应失败均呈现待核对。“保持历史”只记录本页知悉，“去更新”只前往画像，不修改或启动原任务。
3. **P20/P19/P09 日程含义**：跨日、相同起止、DST、离线错过计划与固定间隔锚点有明确说明。历史配置不自动迁移，v1 需执行适配显式支持；详见[日程契约](../../UI_SCHEDULE_POLICY_CONTRACT.md)。没有新增真实调度器。
4. **Windows 交付脚本**：构建绑定完整 ExpectedCommit 与实际 Git 文件字节，构建前后核对；报告保留测试通过/失败/跳过数量。新增可见安装进程、EXE、ASAR 身份核对脚本，人工安装/交互/缩放/卸载仍独立记录为 UNTESTED。Mac 没有 PowerShell，不声称脚本已在 Windows 执行。

## 当前验证

| 检查 | 实际结果与范围 |
|---|---|
| 首次合并全量 | 82 文件：896 passed / 6 failed / 21 skipped；原始失败保留于 `tests-in-progress.log` |
| 修正后全量 | 82 文件：902 passed / 21 skipped，见 `tests-fixed.log`；修正新测试复用已清除标识及旧适配器测试未明确历史日程的错误 |
| 日程详情最后增量 | 3 文件 / 27 passed，见 `schedule-detail.log`；把 P09 配置摘要也接入统一日程说明，不能把该增量说成再次全量 |
| 导航修复后的最终全量 | 82 文件 / 903 passed / 21 skipped，见 `tests-final.log`；此前新增测试误用 Testing Library 不支持的 `exact` 选项导致类型失败，原始日志 `typecheck-navigation-first.log` 保留，已修正后重新通过 |
| 类型检查 | `typecheck-final.log`，在最终 TEST 谱系入口加入后通过 |
| Mac 构包 | `make-mac-navigation.log` 成功；最终包 `0.2.0`，Darwin arm64；先前日程/退出包记录保留于 `make-mac-final.log` |
| 包内检查 | 最终 `package-navigation.json`、`packaged-smoke-navigation.log`、`test-exclusion-navigation.log`，ASAR 冒烟与生产 TEST 排除通过；旧 `*-final` 文件属于导航修正前候选 |
| 原生可见操作 | 新 app、独立用户目录、`yike://app/index.html`；保存 → 离页 → Cmd+Q 提醒 → 继续编辑保留名称和 50 搜贝 → 明确放弃正常退出（exit 0）→ 同目录重启显示草稿 0 |
| 浏览器 P20/P19 | 历史规则显式采用；22:00 / 22:00 阻止下一步；改为次日 02:00、America/New_York 后保存、往返步骤及最终摘要保留；无执行能力时仍不能启动 |
| 浏览器 P09 | `lineage=updated` 只读 TEST 夹具呈现任务 v1 / 当前 v2；“保持历史”后仍 v1；960×600 下文字与操作可见 |

时间控件的浏览器工具 `fill()` 改变 DOM 值但没有提交 React 状态，已记录并用真实键盘方向键提交后验收；不把未触发事件的自动化填值当成产品保存成功。截图首次取样遇宿主缩放裁切，已按实际 CSS 尺寸和稳定绘制重取。临时视口覆盖已清除。

`candidate-local.json` 记录 274 个桌面输入文件的实际字节摘要及最终本地包，属于**构建后的未提交候选盘点**，不是干净提交的构建前后证明。39 个当前 `.vite` 文件逐字节匹配 ASAR。最终 ASAR SHA-256 为 `c96f3b05a1c9139af62e2f34e6990e852090f79eacfccd476bbaf880266ca08b`；完整 ZIP 摘要也在该文件。最终包已实际打开 P20，原生截图 `native/monitor-navigation-final.png` 确认监控侧栏选中和任务标题。完整退出/取消/保留/放弃/重启链绑定导航修正前的 `614396723c2df2bcc61aac6d1c610c3ada7a72bcfdce24933246147264250bfd`，原输入和包摘要保存在 `candidate-before-navigation.json`；最终包另有包内退出冒烟，不能将旧原生链冒充新包重复走查。不把未签名本地 ZIP 当成正式发行包。

## 视觉结论与剩余项

本轮新增版本提醒和日程说明沿用现有 Notice、Field、按钮和排版规范。P09 实图与 R3 原图在同一比较输入中检查，CSS 1484×1060；提醒不遮挡按钮，960×600 也无文档横向溢出。数据状态不同且增加已授权的版本提醒，**不是同状态像素一致 PASS**。原图与当前运行版的字体密度、侧栏宽度、Logo 尺寸和统计区域首屏位置仍有可见差异；后续视觉收口需继续处理，不能因新增提醒可用而关闭这项。

P20 最小窗口日程字段及说明可见，需滚动到保存区；截图见 `browser/p20-960x600.png`。走查发现的监控向导侧栏误高亮“线索采集”及确认页标题已修复。新增测试跨三个步骤与切回单次任务，实际浏览器记录见 `browser/monitor-navigation.txt`；该较早最小窗口图仍保留修复前侧栏。旧测试标签因会话退出保护未自动重载，改用新的隔离会话后明确核对 `aria-current=page` 和最终标题，未将旧页误计为新代码。

新包诊断窗口已实际打开，当前仅支持复制。未配置客户服务的空会话没有可导出的客户记录，因此本轮**尚未完成真实原生文件保存/取消**；不打开假保存窗口或向公开样例补可导出客户权限。

三名子 Agent 在交叉复核前因额度限制停止。主线程已对非作者的 P09 和 Windows 源码做有限复核，日程的遗漏已补；**主线程自己的退出保护、日程增量以及最终整包尚缺独立架构/代码/质量批准，未提交或推送这些改动**。不以作者自检替代仓库要求的独立审核。

后续继续：P04、P14/P15、P18 生命周期的可见验收；最终独立复核；源版本明确的提交/整合/新包；Windows 用户手动回传。已有实际 GET 连接、原始候选和执行接口按最新主线逐一适配，未知能力保持未知。完整 Goal 保持 ACTIVE。

## 主线最新变化

本轮取得并快进至远端 `a77e582`；该范围没有桌面代码差异，保留所有本地桌面改动。新原始候选 HTTP 读取/上传与执行接口已进入后端；`search_suggestions` 仍关闭，交付的是模型/进程/持久化组件，认证 HTTP 路由仍未交付。此判断来自当前主线源码及 Win 交接，不能将组件交接当作可调用搜索 API。

原始候选 `candidate-inbox-v1` 明确不是当前前端已评估 Candidate DTO；接入必须保留 UNVERIFIED、未知发布时间、原文版本和 owner，不能直接映射成可入库的商机。真实接口接入与后端验收仍在原任务卡内推进。
