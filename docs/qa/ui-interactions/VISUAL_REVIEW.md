# 本轮视觉与可见交互记录

2026-09-09，Codex 内置浏览器，隔离 `tests/visual` 服务。合成记录均标 TEST；没有真实采集、发送或客户库写入。参考为 `5929de6` 的 R3 图册。

## 比对方法

图册 P12/P18 原图为 1484×1060。浏览器实际 CSS 视口已通过 `Page.getLayoutMetrics` 校验为相同大小；浏览器自身缩放为 1.5，原始截图 2226×1590，使用系统 `sips` 等比例缩回 CSS 尺寸。未修改页面内容或合成产品状态。带 `-raw` 的原图保留。

已将参考与实际截图放在同次视觉输入中检查：

| 页面/状态 | 实际截图 | 判断 |
|---|---|---|
| P18 未激活、未绑定 | `screens/P18.png` | 保留四区设置行、蓝白导航、授权输入和右侧操作；压缩行距、移除重复说明，保留实际服务/草稿/退出状态 |
| P12 公开研究样例、未选渠道 | `screens/P12-sample.png` | 三列结构及证据顺序保留；样例只读、不可保存或发送。评论/私信用途分开，客户记录与公开样例分开呈现 |
| P12 客户 TEST 商机草稿 | `screens/P12.png` | 检查客户可编辑状态，不以此截图替代公开样例的同状态比对 |
| P12 待确认队列选中记录 | `screens/P12-queue.png` | 两列列表/详情；无多余第三列，记录身份与内容可读 |
| P18 绑定影响确认 | `screens/P18-confirm.png` | 客户空间、目标及复核勾选完整；未勾选不能执行 |
| P18 备份弹窗 960×600 | `screens/P18-backup-960.png` | 文件选择、影响说明、关闭按钮可达；未上传真实文件 |

## 本轮修正与仍有差异

修正了设置页松散行距、备份弹窗连续文本/按钮间距及队列两列布局。截图中可见的整体字号、导航宽度、按钮宽度与图册仍有差异；P12 备注输入与排序控件尚须按完整交互目标进一步补齐。原图的统计数字/示例状态不作为运行事实，不凭空添加。上述差异保留为后续视觉与交互项，不能把本轮检查表述为全部 20 页视觉通过。

## 可见操作

浏览器实际点击 P18 绑定 → 检查影响 → 出现复核勾选与禁用确认按钮；打开备份弹窗及关闭。P12 实际切换待确认、待回复、需处理并刷新；选中队列详情及客户/公开样例草稿。最终队列→草稿的同商机跨用途路由通过真实 AppProvider 测试和浏览器点击核验：由同商机 comment 进入待确认私信记录，打开后草稿箱与私信草稿同时选中，URL 为原商机及 channel=dm；见 `queue-navigation.json`。

两页六视口测量见 `responsive.json`。最小窗口弹窗测量见 `dialog-960.json`。这只是本轮变化的局部证据；全部 P01–P20 同状态比对、原生连续缩放及真实 Windows 验收继续保留。
