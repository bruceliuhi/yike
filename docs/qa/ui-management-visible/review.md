# P18 可见管理状态：独立限定质量复核

2026-09-10。**限定 PASS，本次记录范围未发现 P0/P1/P2 归档阻断。** 此结论针对证据一致性和指定截图可读性，不是管理后台、实际下载/恢复或完整 Goal 验收。

- 冻结 UI 源 `b8b82367b34af24da218dba3796a8945d667aaa6` 与本轮初始主线 `c23380ee61b5dd0e9183b0905067cd0d9b0007ef` 的整个 desktop 树 ID 均为 `cad0f02ae00f60636ac03f5c9a02cac5d262608f`。随后快进 `b7a4c51b3ca659423a61e1350a505284ed77c609` 仅改采集获取脚本、对应测试及整合文档，desktop 仍无差异；未把新来件追认成真实采集能力已验收。
- 读取 01–09、11–12 DOM。导出取消不报已保存；失败提示及重试入口保留；04 的已保存文字明确只属于 TEST 回执。05 的 UNKNOWN 禁用执行、重新预览和回退；06–08 的取消 PENDING、清草稿、再次核对均保留待确认；09 仅选择 CANCELLED 回执后仍有待确认操作；11 产品原操作收到终态后移除；12 恢复预览入口。README 与这些状态相符，没有从 DOM 推断请求次数或幂等性。
- 实际使用图片工具打开 `10-download-pending-1440.png`，并读取 PNG 头确认 **1440×1024**。截图是页面下段，顶部内容已滚出；待确认说明及“核对原操作/取消下载”按钮完整可读，未见横向裁切或重叠。固定 TEST 提示可见，不把此局部滚动状态写成一屏全页或批准图同状态像素验收。CSS/DPR/文档高度是主线程采集记录，本审核没有重新操作浏览器测量。
- `logs/management-tests.log` 实际为 **3 文件、15 passed**；README 没有与旧测试相加，也没有写成全量或真实恢复通过。本审核未重跑测试，仅核对执行日志及范围。新增 `incoming-fetch-contract.log` 为独立的 **1 passed/0.11s**，不并入 P18 的 15 项。
- README 明确全部管理回执为隔离 TEST 内存，没有写出导出文件、实际下载安装或恢复客户库。“文件选择未完成”与主线程报告的 `com.openai.codex` 工具拒绝保持一致；未将组件测试代签可见恢复链，也未主张绕过拒绝。18796 origin 失败及改用允许的 18794 子路径有明确历史边界。
- README 当前 **16 个本地链接均存在**。Windows、真实原生保存、恢复文件可见链、真实服务及完整 UI Goal 的未完成项保留，没有夸大本轮验收。

本审核没有产品修改、新浏览器操作、测试或构包，也没有接触四个无关 rawCandidate 草稿。仅新增本报告，未 Git commit。


## fbcc171 合并后的限定边界补充

本次最终集成为 `fbcc171e002e976daabd6035aaa597d84654168f`，父提交 `729a6b67d094053bd94942d1de10a564ed019e66` 与远端 `ca1f28ac1bdbda3c9222fe22db6e5b688034b729`。主线程报告正常无冲突合并；独立核对父关系、无未合并索引项，且合并结果 desktop 与 ca1f28a 完全相同，没有另外改写来件源码。

**限定集成边界 PASS。** 相对 b7 已有 16 个 desktop 文件差异，主要为 P07 证据/人工来源核验/确认与恢复接线、测试及隔离入口；因此上文“desktop 无差异”的结论只属于当时 b7，不扩至 fbcc171。已核 b8→fbcc171 的 P18 Settings、settings 子模块、management 域/服务、管理 TEST 与 isolation 文件无变化。共享 client 新增 candidateReview 接线、visual main 新增候选配置调用，不是本次重验 P07 或 P18 新构包。

README 末段正确保留旧 b8 截图、浏览器构建和 Mac 包身份，不追认为 fbcc171。新的 `management-tests-integrated.log` 实际为 3 文件、15 passed（07:52:06，1.98s），是合并后另一次限定回归，未与原 15 项相加。类型检查退出 0 为主线程执行记录，空日志不单独产生额外证明，本审核未重跑。

05G 文档确有其作者最终 93 文件、1423 项及 Windows Edge 双视口 TEST 记录；README 只引用对应归属，未由本线程代签，也未把 TEST 浏览器走查当 Windows 安装包、真实平台/模型/客户库验收。更新后 README **21 个本地链接均有效**。没有重跑全套、修改产品或进行新浏览器操作。
