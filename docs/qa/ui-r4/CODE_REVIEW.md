# R4 P12 独立代码复核

2026-09-09。最终候选：`cbc61703347c407413f8a732a6ca9a5637258eba`；主体复核及下述 83 项测试绑定 `190683c87af536b8e1af47e6a002abf0464d376b`，最后两行文案增量另见下节。本记录绑定合并后的候选源码；不把测试适配或截图视为真实短句、保存、发送服务已接通。

## 结论与范围

**限定 PASS：本次独立审查的 P12 增量无剩余 P0 / P1 / P2。** 审查者未编写本次 P12 增量；排除审查者自作的 P10 / P11 研究模块、其视觉适配及既有共享 ledger，实现或平台验收不能由本记录代替。

只读检查了 [useShortCoach](../../../desktop/src/renderer/pages/outreach/useShortCoach.ts)、[AdoptedCoachSummary](../../../desktop/src/renderer/pages/outreach/AdoptedCoachSummary.tsx)、[ShortCoachPanel](../../../desktop/src/renderer/pages/outreach/ShortCoachPanel.tsx)、[ContactEditor](../../../desktop/src/renderer/pages/outreach/ContactEditor.tsx)，并核对 [shortCoach 域校验](../../../desktop/src/renderer/domain/shortCoach.ts)、[保存回执处理](../../../desktop/src/renderer/pages/outreach/useContactDraftSave.ts) 与原 P13 发送门禁的接线。未修改被审源码。

## 核验结果

- 已采用摘要只在当前草稿内容与版本完全相同、服务对象相同、用户及账户空间相同、来源/画像/用途/渠道绑定相同、建议未过期时展示。来源键包含原文 URL、摘录、来源版本、观察时间及核验状态。条件变化立即停止展示并清除记录，切走再切回不能恢复旧摘要。
- 保存只同步原提交文字的 `savedContent`，不会把未编辑的已采用文字误判为新版本。编辑会增加草稿版本并清原确认，即使随后手工恢复相同文字，也不会复活原检查摘要。用途、渠道、来源或空间切换同样失效；过期有独立定时清除。
- 摘要和依据弹窗明确说明这是采用时的原检查记录，不代表后来重新审核；引用与建议文字均保留。没有以摘要存在来放开发送。
- 发送对象和账号置于原生 `details` 内，折叠不会清除受控值或创建新的发送身份。对象映射、已连接渠道、保存状态、最终确认、原请求未决保护仍由原路径核验；样例仍不能保存或发送。

## 本轮已修问题

此前独立检查发现一项 **P1 账户空间隔离问题**：同一用户在新空间主动核对旧空间的保存请求时，合法旧回执可能更新当前草稿并显示成功。现有 `useContactDraftSave.accept` 在应用快照与成功提示前精确比较空间 ID、版本及缺失情况。可核销原请求的合法终态，但不把旧空间内容应用到当前空间；回归覆盖空间 ID、版本、缺失三种变化。

另有一项 **P2 依据表述问题** 已修：`CONTEXT` / `PROMISE` 检查标为 `SUPPORTED` 时必须有合法原文引用锚点，不能用无引用的承诺形成“建议依据”。纯长度规则及明确待复核状态不被错误要求附来源事实。

本次新增的已采用摘要与配置折叠，在上述修复基础上复核，未发现新的状态或发送门禁回归。

## 独立运行证据

在 `desktop/`，使用 Node `24.19.0`，实际执行：

```sh
npm test -- tests/ui/r4-short-coach.test.tsx tests/ui/r4-short-coach-domain.test.ts tests/ui/send-confirmation.test.tsx
npm run typecheck
```

结果：**3 个测试文件、83 项通过**（工具输出 `aa3202`）；类型检查通过（`07e581`）。包含新增 7 项采用摘要/折叠回归：保存后摘要保留、编辑再恢复同文不复活、用途/渠道/来源/空间/过期变化失效、折叠后发送配置值保留。没有把作者报告的 7 文件 131 项与本次 83 项相加，亦未重复运行全仓套件。

## 最终候选的两行文案增量

已独立核对 `190683c..cbc6170`：只在 [公开研究样例](../../../desktop/src/renderer/pages/opportunities/OpportunityEvidence.tsx) 修改 `comment` / `dm` 两个文字值。评论改为询问展区设计搭建技术资料和服务范围说明的获取方式；私信只增加“高交会展区预算询价”的现有来源上下文，随后提出同一个资料获取问题。没有新添采购承诺、供应商资质、真实回复或成交事实。

`id=sample`、`sample=true`、未绑定画像和待复核状态均未变化；只读、禁保存/发送/入库的逻辑亦未改。本次增量是主线程修改的展示文案，未借此重审或自审通过 P10/P11 的研究实现。**原限定结论可适用于最终候选**；没有为两行文案重新报告测试，83 项仍只记为主体提交上的实际运行证据。

## 剩余边界

本记录只证明所列客户端状态逻辑和测试路径。最终视觉效果由本轮同状态截图另审；真实短句生成、可信账户范围、保存回执和外联渠道仍需生产适配及目标环境证据。Windows、费用结算、真实消息送达和上线结论不在本次复核范围。
