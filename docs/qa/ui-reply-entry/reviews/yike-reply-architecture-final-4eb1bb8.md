# P14/P15 最终增量架构复核

## 结论与绑定

**PASS：本次有限差量复核未发现未解决的 P0/P1/P2；新增的延迟列表覆盖本地选择问题已关闭。** 可继续最终候选的可见验收与交付检查，不代表完整 Goal 或生产回复服务验收完成。

- 最终候选：`4eb1bb88b97d5f1d9ff3db569d80f9d9e79855b4`
- 直接父提交：`ffd7d80ea28b54d9432cd5a4dcad72c1ddf7b05e`
- 原回复入口候选：`53d506b7ba439facda0d7725a72f8c1c2ee945a5`
- 原功能审核基线：`dd09d0fbd4e900b020e41a8486f0343ff9f33c56`
- 仓库：`/Users/bruce/Developer/work/yike-ai-product-design`

本次只读审核 `4eb1bb8` 的两个文件差异：`desktop/src/renderer/pages/Followups.tsx` 与 `desktop/tests/ui/followup-reply-entry.test.tsx`。审核者不是这些差异的作者；没有改源码、提交或重新运行测试。

## 保留原审核与新增发现

原报告 `/tmp/yike-reply-architecture-53d506b.md` 保持原样。它记录当时精确商机读取、无人工登记回复入口、目标/空间隔离及添加/取消路径的限定检查，实际独立运行 **3 套 / 21 passed**，日志 `/tmp/yike-reply-architecture-53d506b.log`。

后来 Peirce 的独立补测发现一项原 21 项没有覆盖的 P2：从带目标的深链进入后，人工记录列表仍在读取；用户已经修改日期或右侧商机选择，列表迟到返回时，原深链聚焦 effect 仍会清空日期或重新聚焦原记录。此前 PASS 仅表示当时范围内未发现问题，不能解释为已经覆盖这个时序反例。

## 差量核对

1. 路由 intent 变化时，在对应 intent effect 中重置筛选、选中记录和聚焦状态，同时重置 `handledIntent`；重置不再等待人工列表返回。右侧对象原有的渲染期 intent 判定保留。
2. 迟到列表的自动聚焦 effect 不再清空负责人或日期，只在该 intent 尚未处理时定位目标记录。
3. `localSelection()` 立即将当前 intent 标为已处理。用户改日期、标签、负责人或本地选行后，旧列表不能覆盖这些选择。
4. 右侧商机选择也调用该本地选择门禁，并清除旧左侧选中记录，随后更新独立回复对象；迟到列表不再把原深链行画成当前选择。
5. 本次没有修改精确 detail 授权、请求取消/空间隔离、跟进持久锁或 hash ACK 协议，也没有修改后端、平台收发或 Windows 入口。

新增的 5 项测试直接覆盖：已读/撤销预检离开目标后不得派发（2 项）、已派发已读请求迟到后保留原锁（1 项）、延迟人工列表不得覆盖日期/标签（1 项）、更换回复对象后不得重新聚焦旧深链行（1 项）。最后两项针对新增 P2，前面三项加强既有请求边界。原 entry 套件由 13 项扩展到 18 项。

主线程转述 Peirce 的最终独立定向结果为 **75 passed**；本报告未重复运行该组测试，也不与原 21 项相加。原 `ffd7d80` 的后端/文档合并一致性结论仍适用，因为 `4eb1bb8` 只包含上述两个前端与测试文件差异。

## 证据限制

此前 `ffd7d80` 全量 **1248 passed / 22 skipped**、Mac 构包和 smoke 属于修复前候选，不能直接当作 `4eb1bb8` 的最终构包或可见验收。本报告不新增后台、真实消息、生产授权、完整视觉或 Windows 验收结论；这些应由主线程对最终源码、最终包和实际环境分别绑定证据。
