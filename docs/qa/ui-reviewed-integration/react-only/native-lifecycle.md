# 修复包实际原生操作

2026-09-10 01:17–01:20，Mac arm64。直接运行构建后的 .app 可执行文件，不使用开发服务器，不替换 dialog，未配置客户服务。

- ASAR：`befacea1993eda9673e872a16a950e8eb76a587cfb181b3c7129bcd6a466620a`。
- 使用独立 user-data-dir `/tmp/yike-native-fixed-20260910`，进程 86448。
- 冷启动真实显示工作台、机会简报与明确的公开研究样例，未出现页面错误。
- 进入监控→新建任务，输入“本机验收任务”和 50 搜贝，保存为本机会话草稿，显示未启动及关闭会清除。
- 返回监控列表后 Cmd+Q，出现真正的原生警告；点“继续编辑”，从列表恢复草稿，名称和 50 搜贝均保留。
- 再 Cmd+Q，点“放弃更改并关闭”，命令进程退出码 0（exec 6f1225）。
- 同包同 user-data-dir 重启进入工作台，再进监控，显示本机草稿 0、无验收任务。

截图和 AX：native-workbench、native-saved-draft、native-quit-warning、native-continue-preserves-draft、native-restart。截图证明对应状态可见，不代表全页像素一致；未执行真实研究、消耗搜贝、平台登录或客户操作。原生文件保存选择器、Windows 生命周期和签名发行不在此记录范围。
