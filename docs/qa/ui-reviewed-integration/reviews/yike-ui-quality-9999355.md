# UI 候选独立质量复核

日期：2026-09-10。冻结提交：`99993552f1559681c8f671a57096ec23d4e9095b`；基线：`a77e5828de9ecb0f3c9b60dfcebf56c96685d9bc`。只读工作目录：`/tmp/yike-ui-review-20260910`。

**判定：本次限定质量范围 PASS，可合入前端增量；未发现新增 P0/P1。** 这不是全部 UI Goal、Windows 发行或真实业务闭环完成结论。Windows 构建/来源/安装核验脚本由本人此前实现，明确排除其独立代码批准；应采用其他审核者的结论。也不重新批准本人此前参与的基础 hooks/ledger/商机模块；本轮只审其他作者新增的会话退出调用及相应增量。

## 独立执行与边界覆盖

使用 Node 24.19.0，临时链接原工作区的 `desktop/node_modules`，在冻结树实际执行：

```sh
npm test -- tests/ui/session-draft-exit.test.tsx tests/ui/task-profile.test.tsx tests/ui/schedule-policy.test.ts tests/ui/schedule-policy-ui.test.tsx tests/ui/connection-registry-client.test.ts tests/ui/connection-registry.test.tsx tests/ui/connections.test.tsx tests/ui/connection-disconnect.test.tsx tests/ui/navigation.test.tsx
npm run typecheck
```

结果：**9 文件，109 passed，0 failed，0 skipped**（工具 `89fdcf`，00:50:33）；类型检查通过（`72dfb3`）。没有重跑全仓或 Windows 条件测试，不把历史计数相加。

| 增量 | 核验到的真实边界 | 仍不能据此宣称 |
| --- | --- | --- |
| 会话任务草稿退出 | beforeunload 事件边界同步读取最近编辑；已离开编辑器的已保存任务库/模板仍提醒；空初始草稿不提醒；普通站内导航不被任务库阻拦；主动清除、删除存储失败墓碑、内存副本与 StrictMode 清理有实际断言。原生提示明确关闭会清除会话草稿。 | 草稿跨客户端重启持久化；也不是所有产品表单的全组合退出验收。 |
| P09 画像对照 | 任务版本行 ID、版本号与服务端稳定业务实体共同匹配；另一业务的大版本、缺关联、重复确认、逆序关系均不推断当前版本。失败/超时/迟到及同用户 A→B→A 空间切换不恢复旧知悉状态。保持历史仅本页知悉，去更新仅导航，不调用任务写操作。 | 已切换旧任务画像，或服务缺完整关联时仍可判断更新。 |
| P20/P19 日程 | 跨日/不含结束、相同起止阻止前进且保留输入；历史草稿进入不改指纹，明确采用才换 v1；保存、步骤往返与确认保持配置。无执行适配声明不启动，延迟预检后声明撤回也不派发/不写启动未决锁；旧未版本化路径有独立回归。 | DST 调度器、离线补偿或真实监控执行已实现；这些测试验证配置语义和门禁，不是在执行时间调度。 |
| 连接登记与隔离 | 固定认证 GET 与 IPC 白名单，不允许调用方传租户/路径/认证头；解码必需 ID/设备/版本/时间、拒绝重复/坏响应，真实空列表与失败分开。同账号两个设备分别呈现并按 connectionId 取详情；刷新隐藏旧详情；切账户空间关闭详情并忽略旧响应。登记的 CONNECTED 不补 capabilities；旧平台断开/任务选择不能把登记当执行授权。 | 真正平台登录、设备版本化断开、平台能力核验或采集/发送已经接通。 |

现有 `pilot/ui_api.py` 的认证 GET `/connections` 与客户端字段映射一致；本次没有运行真实受限 PostgreSQL/登录会话的端到端读取，不以 HTTP mock 或 TEST 列表代替这层验收。

## 构建、字节和日志证据

独立读取最新 `docs/qa/ui-connection-registry/candidate-local.json`，逐项计算冻结树输入摘要：**279 项全部匹配**；其中生产 src/build/assets 与主要构建/锁文件所需 **152 项无缺失**（工具 `890704`、`24ccaf`）。这把已记录的构建后输入盘点绑定到本次冻结提交；它仍不等于补做了构建前后干净 Git 证明。

实际读取原工作区现存产物，核对大小和 SHA-256：

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| app.asar | 1,768,929 | `6e2edbd31e149fce621311f2eed26845deee9bc4eae7ae4bf746c79a654f5427` |
| Mac arm64 ZIP | 122,472,107 | `780ac67bfde60adebaa0ccf68b36c9e3956736e0cc676c076914f0b831cf834c` |

ZIP 中恰有一个 `Contents/Resources/app.asar`，解包字节摘要与上表相同。再独立读取 ASAR：当前 **39 个 .vite 文件**逐字节相同，package.json 记录的 **36 个 renderer 资源摘要**全部相符（`2813d7`）。没有重建包或重新执行原生冒烟。

已核对日志：

- 日程/退出/导航集合最终为 82 文件、903 passed / 21 skipped；连接集合的后续全量为 84 文件、924 passed / 21 skipped。后续错误提示修订单列 5 文件、43 passed，没有谎称修订后再次全量。
- 最新连接候选 `make-mac-final.log`、类型日志、包结构报告和包内冒烟记录与当前包范围一致；冒烟明确使用隔离临时目录和替代文件选择器，不能当作人工原生保存/取消通过。
- 生产排除日志明确记录 `productionGraphModules=4759`、夹具引用 0、已检查现有 ASAR、failures=[]。前面的 Vite transformed4760 是另一统计字段，不是矛盾。
- 首次六项失败、类型失败、旧包和旧截图保留。28 个相关 Markdown 本地文件链接均存在；本次没有重新采集任何截图。

## 可见证据与剩余项

实际查看七张图：原生退出警告、继续保留草稿、明确放弃后草稿为0、最新连接未配置页，以及 TEST P09 更新提示、P20 最小窗口、P16 最小详情。

截图与文字说明一致：退出提醒清楚指出会话草稿会丢失；继续编辑图仍显示测试名称与50搜贝配置；重启图显示草稿0；最新原生连接页是服务未配置/待读取，未伪造真实账号。P09 图有明显 TEST 标记，任务v1/当前v2仅是测试谱系。P20 最小窗口图可读配置说明，但未显示保存底部，且保留已说明的旧导航高亮；不能据此称当前整个长页一屏通过。P16 最小详情截图存在明显宿主捕获模糊，只可佐证弹窗大体位置；文档已保留清晰度限制，不将它计为字体/细节视觉验收通过。

完整可见“保存→离页→退出取消→继续保留→放弃→重启”链明确绑定较早 ASAR `614396…`；导航修正后的 `c96f3b…` 另有新页/包内退出冒烟；最新连接包为上表 `6e2edbd…`。报告没有把较早原生完整链冒称最新包重复走查。最新候选仍需相应完整原生流程、真实文件保存/取消及剩余页面状态验收。

非阻断的交付文案收口：两份现有 README 的“未提交/独立 Agent 因额度停止”属于写入时点，最终交付索引应追加本次冻结 SHA 与实际独立审核结果，保留历史而非继续作为最新状态。本人未改这些文档。

真实连接/画像/执行服务端整链、平台登录/消息/搜贝计量、P04/P14/P15/P18 剩余可见生命周期、Windows 新候选安装/退出/重启/卸载及100/125/150系统缩放、正式签名/公证、生产恢复和其他原有门禁均保留。**本报告允许本范围代码合入，不关闭整个前端 Goal，也不批准对外生产上线。**
