# 截图采集校准记录，不能作为多分辨率视觉通过证据

首轮 Browser screenshot 在切换 viewport 后出现像素尺寸与 CSS 视口失配、局部缩放或裁切。独立审核发现并排除了后四张。CSS scrollWidth 等于 innerWidth 只能证明测量时没有文档横溢，不能代替完整取图。随后直接 CDP 每页 emulation/captureScreenshot 校准，首次同步读取还读到了过渡布局；这些图片仅保留排查，不用于最终视觉结论。最终图与实测尺寸另存主目录。

原来的三个 `*-1280x720.jpg`（P06 返回、P14匹配、P14首次保存）实际 JPEG 是1024×576，已保留原字节并改名 `*-initial-1024x576.jpg`；它们的 DOM 操作记录有效，但不证明1280分辨率。P04前后两图经当时DOM及独立视觉核对，仍保持1280×720原范围。
