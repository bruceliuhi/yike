# 本机资料、任务返回与回复入口：最终文档与证据一致性审核

日期：2026-09-10。独立审核对象为 `docs/qa/ui-visible-local-handoff/` 当前冻结记录；主线 `d5ee8c27532ff15436eaa2bcfeafca1a9e0e86a0`，产品和 Mac 包候选 `8af8eafba0faef68ae48ea76f3d5d4a70c7b06c8`。

**结论：本批文档与证据一致性限定 PASS。未发现应阻断本批证据归档的 P0/P1/P2。** 本结论不代替逐页视觉、全部交互、真实后台、Windows 或完整 Goal 验收，也不覆盖随后新远端合并。

## 本次独立核对

- 读取最终 README、校准记录、五尺寸独立视觉报告及被链接的来件兼容报告更正。对前三份入口的 Markdown 本地引用实际检查 **51 项，0 缺失**。README 的图片扩展名、早期 1024×576 尺寸、校准目录与最终清单一致。
- `git diff 8af8eaf..d5ee8c2 -- desktop` 为空。README 将 342 项输入、实际 ASAR/ZIP、33 项针对性测试绑定 8af；将此前 1340 passed/23 skipped 全量绑定 d816，没有宣称在 8af 重跑。原生进程及产物字节的独立检查沿用本批已归档的 `yike-visible-local-evidence-review.md`；本次未重复构包或全量测试。
- 最终 `p14-final-responsive.json` 五个文件均实际存在，PNG 编码、IHDR 尺寸、SHA-256 与清单完全相同；各 CSS 宽高与 PNG 相同且 scrollWidth=width。最终 2560 图已恢复原路径，SHA 为 `0d119999091e0556876ce62e7033076a415b56acf43413f09780c530c25e3913`。先前短暂缺图源于校准/重采的中间状态，冻结后已关闭。没有仅凭文件头追认视觉通过：实际看图结论属于另一个非作者的五尺寸报告。
- 来件报告已就近更正旧 804 归属：旧日志是 file not found/no tests；有效四文件 **32 passed / 0.14s** 是本次 d5 新执行。两者未合并或追认为旧结果，也未写成真实 PostgreSQL 验收。

## 范围表述

- P04 交互链属于先前 fca 已加载构建；8af 仅收敛空同步列表布局。README 明确旧图与新 compact 图的身份，不以新构建清单回绑旧操作。
- P06/P09 返回证据保留具体任务、步骤、tab 和平台参数；没有把连接弹窗取消、失败 goto 或没有发生的登录/运行写成成功执行。
- P14/P15 是显著 TEST 的隔离内存回复和人工登记。已读不制造人工行、未匹配独立、取消保留目标、切“全部”才看到唯一人工事实等描述与 AX 记录相符。五尺寸仅覆盖同一个“全部/一条人工登记/匹配回复已读”状态，不扩大至长列表、弹窗、错误态或 Windows 系统缩放。
- 原生顺序明确为冷启动→输入→关闭提示取消→保存会话草稿→明确放弃关闭→进程退出→同路径重启后新任务为空。没有把退出清除会话草稿写成跨重启持久化；初始 AX 是增量 diff，真实文件选择器未执行，smoke 替身不能代签。
- P11 当前合成快照与机会头部业务内容不同，README 已显式标明仅用于组件状态覆盖，不可当一致真实案例或与批准样例抽屉同状态的整页设计验收。
- 校准失败、旧 804 失败及测试候选差异均保留；日志归档仅清理空白的行为已披露，不写成未处理原始字节。四个 untracked raw 草稿未计入交付能力。

本次没有操作 CUA、修改产品代码或截图，也没有重新运行测试、构包或外部服务。完整 05A/Goal、未覆盖页面状态、真实服务、原生文件选择器、Windows 安装/缩放/卸载及生产部署仍按 README 保持待验。
