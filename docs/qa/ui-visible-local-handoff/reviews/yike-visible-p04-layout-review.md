# P04 本机草稿首屏布局：独立视觉复核

日期：2026-09-10。

**限定结论：PASS。** 在提供的同视口 1280 × 720、同步资料为空且本机有一份草稿的 TEST 状态中，compact 改动已将草稿内容和“带入当前画像”操作放入首屏，未观察到新增明显布局缺陷。这不是全 P04 的批准设计符合性、交互或生产验收。

## 输入与方法

独立使用 `view_image` 实际查看两图；读取 DOM 佐证及图片尺寸/编码/哈希。未操作 CUA、未运行产品或重做测试。

仓库：`/Users/bruce/Developer/work/yike-ai-product-design`。

- 改前，主线程绑定 fca：`docs/qa/ui-visible-local-handoff/screenshots/p04-local-drafts-1280x720.png`。
  - 实际尺寸 1280 × 720，SHA-256 `e35ce41970b110a69e108e33634520dbb730d72c9b90a0d52d37023f2db37c44`。
- 改后，主线程绑定 `8af8eafba0faef68ae48ea76f3d5d4a70c7b06c8`：`docs/qa/ui-visible-local-handoff/screenshots/p04-local-drafts-compact-1280x720.png`。
  - 实际尺寸 1280 × 720，SHA-256 `25749ef54848e28cd27ab7f95b3ead9972b808ba2562ef38865f3672c22f6214`。
- DOM：`docs/qa/ui-visible-local-handoff/p04-compact-layout.json`。

以上文件路径按复核时实际文件名记录；图片编码实际均为 **JPEG**，虽然后缀是 `.png`。这不阻断本次看图判断，但归档应更正后缀与引用，或明确真实编码；不得据后缀声称是 PNG 原始采集。

## 直接观察

改前的“暂无资料”大空态占据主要内容区，本机草稿标题约在 y645，首屏只露出其标题、说明与表头，资料行及带入操作在视口下方，用户需向下滚动才能操作。

改后空态压为一行“当前画像尚无已同步资料，可从下方草稿带入”。草稿区域约在 y400 开始，完整资料行约在 y481–549。资料名称、用途、引用范围、状态以及“编辑 / 删除 / 带入当前画像”都在首屏可见；标题、表头、资料与按钮之间有清晰间距，未见文字或按钮重叠、内容裁切。原侧栏、页面标题、页签与添加资料按钮未出现新的明显偏移。

DOM 佐证：“带入当前画像”矩形为 x1052.81 / y495.80 / w97.60 / h36，底边约 531.80，小于视口高 720；`scrollWidth = width = 1280`，未报告横向溢出。记录 dpr 为 1.25。DOM 只作佐证，视觉结论来自实际截图。

## 限定边界

该本机草稿状态没有对应已批准的独立参考图，所以只判定本次首屏可发现性与明显布局缺陷，不能标成“完整 P04 设计 PASS”。截图有 TEST 隔离标记，资料为 TEST 内容，不是客户资料或生产服务成功。

本次未独立验证按钮点击、取消/保存/重复带入、键盘操作、长资料名、多资料、加载/失败/解析中状态、其他视口或 Windows。那些能力及证据应沿用各自精确测试/可见记录，不以两张静态截图替代。
