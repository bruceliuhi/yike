# P14 已登记一条人工记录状态：有限视觉复核

日期：2026-09-10。

**结论：1280 × 720 状态有限通过；其余四张证据需重新采集，不能据此批准五种分辨率的完整视觉表现。** 问题目前属于截图与 DOM 测量不一致，不据此判定产品存在横向溢出，也不要求先修改产品代码。

## 范围和方法

仓库：`/Users/bruce/Developer/work/yike-ai-product-design`。

使用 `view_image` 实际逐张查看 `docs/qa/ui-visible-local-handoff/screenshots/p14-saved-*.jpg` 五图，读取 `p14-responsive.json`、三份相关状态文本，并对照 `design/v02-suite-r3/DESIGN_SYSTEM.md` 的共同骨架、轻量分隔、蓝色主动作及标签规则。未控制 CUA，未运行产品/测试，未独立重演主线程的已读、添加、取消、保存链路。

图片内容为 TEST 内存回复及人工记录，截图顶部有清晰隔离标识。没有同状态的 approved reference，因此本报告不是全 P14 设计符合性批准，也不把 TEST 已读或登记计为真实平台/客户业务成功。

## 五张实际文件检查

| 文件名中的目标尺寸 | 文件实际尺寸/编码 | DOM 视口 / scrollWidth | 实际看图结果 |
|---|---|---|---|
| 1280×720 | 1280×720 JPEG | 1280×720 / 1280 | 完整。筛选、日期、刷新、添加跟进、唯一记录、回复选择器、刷新回复与两个用途标签均可见。未发现明显重叠、裁切或横向溢出。 |
| 1366×768 | 1365×768 JPEG | 1366×768 / 1366 | 右侧刷新回复及关联面板右边缘被图片裁掉，无法确认右侧操作完整可见。 |
| 1440×1024 | 1440×810 JPEG | 1440×1024 / 1440 | 高度与文件目标不符，右侧回复面板被切，不能作为完整 1440×1024 视口证据。 |
| 1920×1080 | 1904×1080 JPEG | 1920×1080 / 1920 | 右侧刷新回复不在画内，正文右端被切；底部导航也未完整进入图片。 |
| 2560×1440 | 2539×1440 JPEG | 2560×1440 / 2560 | 关联回复面板大部分在画外，缺失主要右侧操作；不能据此确认大屏双栏完整布局。 |

文件尺寸差一两个像素本身未必构成实质问题；本次关键是右侧内容明显缺失，且固定侧栏在截图中随目标尺寸被放大，而 DOM 仍按固定 208 CSS px 布局。DOM 中回复选择器右边界分别为 1255.99、1338.39、1411.99、1879.99、2263.99，均落在各自视口内，与后四张图中的裁切不一致。它提示截图缩放/裁切与 DOM 测量未对应，不能用 `scrollWidth == innerWidth` 替代缺失的可见证据。

## 1280 状态与设计系统

已完整可见的 1280 图保持浅灰左导航、蓝色选中项、白色主背景、文本加细分隔线的层级；主要“添加跟进”使用蓝色，次要刷新为低强调控件；负责人、日期及查看商机回复均有常驻标签。左右两栏和记录行未重叠。窄表中的“1 条通道回复”和成员名会换行，但仍完整可读，未遮挡操作。

DOM 中选择器高 36 CSS px，与既有桌面控件目标一致。这里只确认可见风格方向与少量已测几何数据；未做色值/对比度、字体逐像素、触控命中范围或键盘顺序测量，不能把这些称为通过。

## 状态证据的归属

`p14-matched-read.txt` 显示无人工行时已选择匹配商机且关联回复为“已读”；`p14-add-cancel-retained.txt` 显示取消添加后原目标仍选中，列表为空；`p14-first-manual-saved.txt` 显示“全部”中一条记录且原目标仍选中。这些支持主线程所记录的不同状态，但由主线程执行，本审核未独立点击重演。

“无人工行→匹配回复→标已读→未匹配仍未读→添加取消→再添加保存”的完整顺序及外部 IO 隔离仍依其原始执行记录，不由五张最终静态图单独证明。

## 下一步和证据身份

保留本次原始图片及失配记录，重新采集后四张完整目标视口截图，确保图片实际尺寸/比例与测量相符，页面左右边缘和操作均在画内。再做有限重看即可，不需要为截图证据问题重复全量测试或改产品逻辑。

本次检查的图片 SHA-256：

- 1280：`4d5eb4a83c1647de7cf78f4392fd8f7168ed4fe7a5c6c62857d6fe358ad9d6dd`
- 1366：`4c968584a0c984b36077559c3ee177864f046de059a9f7c8563c19423050880e`
- 1440：`c3f3359a90fd27d1300af6068d1ea4e45d503cff9449e50c057586210e08aa49`
- 1920：`e8cc5fbcf465ac8544d7f57221e0659ea372159a0500704b2159dfeead7e2c99`
- 2560：`51920d32fa53de06799d26bb0f20effb5643a2a9f8bcd442c984a8b63745386d`

上述范围不包括其他状态、长列表/长正文/错误、模态框、Windows 缩放或生产回复服务；完整 Goal 不因此完成。
