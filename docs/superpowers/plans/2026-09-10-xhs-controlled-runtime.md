# XHS controlled runtime and source consumption

**Goal:** Extend the pinned governed runtime and Windows source driver to the primary XHS channel; preserve exact comment evidence, bounded collection, isolated profiles, and honest failure/stop semantics. This is not live account or customer acceptance.

**Architecture:** Add a second governed patch after the existing bili/dy patch, keeping the same upstream pin and dependency lock. Patch XHS to use bundled Chromium/default UA/private YIKE_PROFILE_PATH, no CDP/stealth/proxy/automatic challenge solving, progress and explicit terminal classification. Correct search pagination (including the last page) and shared parent/reply budget; do not suppress exceptions as empty success. Raw writer preserves original text/title/nulls, converts known millisecond publication fields explicitly, removes access tokens from stored URLs, and uses unknown parent/author correctly. Existing strict reader and Windows driver then accept XHS and preserve its alphanumeric IDs.

Alternatives considered: separate crawler duplicates existing capabilities; weakening readiness or treating unsupported output as empty would not deliver the requested main channel. Use the existing governed implementation instead. Win owns the entire remaining chain since user's Mac takeover instruction; no external ACK wait.

## Tasks (subagent-driven-development, TDD)

- [x] Runtime helper owns new `vendor/patches/mediacrawler/0002-yike-xhs-runtime.patch`, `vendor/mediacrawler.lock`, and new `tests/test_xhs_governed_runtime.py`. Work on a separate local upstream checkout (never mutate installed runtime); tests execute actual patched methods under controlled transport/browser fixtures, not just grep. Enforce profile/default browser contract, correct limit/last-page behavior, no swallowed failures, observable login/verification/rate/permission/network/shape outcomes; raw stored evidence and no token logs. Mechanically regenerate patch SHA, patched files/tree hashes and patchset SHA from applied bytes. Keep existing dependency and pin values unchanged.
- [x] Root owns `app/collection_output.py`, `app/windows_source_driver.py` and dedicated XHS output/driver tests. Extend exact platform/file/ID mapping for `xhs/jsonl`, `note_id` and XHS comment IDs. Do not loosen numeric bili/dy, normalize raw text, accept conflicting IDs or invent author/parent data. Use fresh new runtime governance only; old installed receipt must not imply new patch installed. Real Windows fixture source→reader→formal mapper test with Unicode/parent/unknown timestamps.
- [x] Combine targeted tests, independent spec then code/architecture batch review once, only fix deltas. Validate actual patch applies to clean pinned source and hashes agree. Retain existing installation untouched; prepare a new governed installation only if needed for actual probe, no repeated full download for doc changes. No account login/network send during tests, no enabling backend capability until complete main/lease driver bridge and live verification. Short main handoff with remaining exact blockers; no full-suite/build cycles.

Base main `5f8bc37`. Existing process/identity/worker evidence reused on identical bytes. Approved autonomous goal supersedes repeated design permission prompts. Node/main bridge and real source/authorized account verification remain subsequent integration, not redefined out of the goal.
