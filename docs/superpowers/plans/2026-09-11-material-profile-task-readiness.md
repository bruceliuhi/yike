# 资料画像到任务策略：兼容修复

基线 `83a52c6`。执行已批准资料引用与任务主流程；使用系统化排错及定向TDD，不新增产品范围。

根因假设：PilotStore的新画像摘要包含引用身份（明确人工解除后也使用空引用包装）；ResearchStrategyStore._profile_state仍只计算payload摘要，因而把合法画像误判无效。带锁prepare/confirm/resolver以及只读read_snapshot都调用旧判断。

本批步骤：

- [x] 受限PG复现真实代码READY流程→采用→保存/确认画像→准备/确认任务策略失败，以及空引用人工画像同样失败（输入为合成资料）。
- [x] 最小兼容校验：同租户最小资格行参与摘要验证；无引用时兼容旧裸payload和新空包装；有效非空引用必须完整摘要一致，失效引用不得支持新策略确认/解析。保持原画像/策略/请求快照与历史回执，不删除历史或改写旧配置。
- [x] 两条消费路径（带锁与只读）共用判断；补当前策略应用角色对资格列的最小SELECT，不读私有原资料或授予新写权限。旧任务取消/策略撤销路径保持原样。
- [x] 定向验证资料/人工/旧格式→策略、来源撤销后不可再执行但原回执可读、撤销可用；一次独立整批审核，不全量/构包。

只验证此主流程阻断，不把离线资料fixture称为真实客户资料或采集成功。后续行业策略正式采用、真实平台、Windows/生产/UAT仍需完成。

## 实施与验证

源码 `ecb5d1a` 独立整批 GO，无可操作发现。已定位并修复摘要兼容与引用资格读取，两条消费路径共用校验。受限本地 PostgreSQL 定向测试：修复前 3 failed / 1 passed（合法资料画像与空引用画像被误拒）；修复后 4 passed / 2.23s，覆盖资料采用、旧格式、空引用、来源撤销后的资格与历史回执；py_compile、diff检查通过。

测试夹具已有资料资格SELECT，列级最小授权只做独立静态核对，不称独立部署权限验收；未重复测试、构包或真实平台运行。不代表完整V0.2、Windows、生产或商业验证通过。后续继续策略正式采用与实际判断链。
