# 单次多平台采集与原任务续接

基线 `09ad94bb7bd5c29f03bede29e207874109c63a4a`。落实 AUTHORITY 已批准的多平台任务主链，不新增平台、后台权限或外部发送。

## 约束

- 复用既有三平台监控串行执行机制，单次任务允许三平台模式下小红书/抖音/B站的有序子集；旧 xhs-only 模式仍仅一个小红书。确认策略平台顺序、全部账号/设备/版本及快照摘要必须吻合，启动前全部检查。
- 单次/监控共用一个执行槽；各平台一个原 platform_run，一个原 CLAIM，不并行开浏览器。后一个平台开始前重新核对原用户/会话/设备/账号；取消、身份变化、未知结果、物理停止未确认均停止后续执行。
- 一个任务的 max_records 为所有平台共享上限。按确认顺序固定均分（余数给前面的平台），不返还过滤/重复/未使用额度；最小上限不得小于平台数。每个平台 worker 使用自己的分配上限，但原确认快照不改。单平台旧行为不变，监控使用相同分配规则。
- 多个原上传批次可以依次恢复回执/FINISH；只恢复当前任务、原平台run、原设备/凭据版本的不可变批次，拒绝重复/陌生/冲突映射；RECOVER 不打开浏览器，不生成新采集请求。原 UNKNOWN/404 不证明未执行。
- 原 START 显式重试可继续安全未执行的后缀：已完成前缀必须有当前服务端 SUCCEEDED、对应本机原 CLAIM/上传/FINISH，并核对真实原回执；未执行后缀必须当前 generation=0/PENDING 且无任一非START原操作或批次。任何缺失/未知/不一致不开始新来源。不能重新执行已 CLAIM 平台；原任务到期/撤销/取消/账户变化仍由当前核验拒绝。监控轮次不提供手工来源续接。
- 客户详情页仅提供主动确认后的“核对原上传”和“继续未执行平台”，使用现有固定IPC，不新造后端协议。先同设备核对/原请求读取，再使用原START的requestId重试；页面状态切换/退出后不得继续。恢复成功不能夸大整任务完成。
- 不做全量回归或重复构包；新增行为的最小失败→通过、一次独立全批审核、一次renderer build。无真实平台/模型/消息/生产调用。

## Task 1: 原生执行、预算与恢复

负责人：实现agent，仅改 `desktop/src/main/foregroundCollectionController.ts`、`desktop/src/main/collectionWorker.ts` 及其针对性测试（如需额外native文件先通知根）。读取本文件“约束”并完整遵守。

将已有startMonitor的串行生命周期抽取成小型共享函数，供once与monitor调用，不重建执行器。worker新增可选严格验证的平台预算参数，默认保留单平台行为；controller计算固定分配并传入。实现多批次只读/原写入恢复，以及resumeStart原任务安全后缀续接。状态显示不得把非最终平台COMPLETED冒充整任务完成。

最小测试覆盖：三平台实际顺序/独立profile；共享预算且不能低于平台数；取消/账号变化停止后续；第一平台完成后第二平台上传未知；全部原批次恢复不启动driver；有证据前缀+generation0后缀可显式继续、存在CLAIM或缺证据时拒绝；monitor路径预算复用。测试复用合成driver，明确非真实平台证据。

不改renderer。先失败用例再实现，定向测试及tsc；不构包/不push/amend。提交前通知root，仅显式stage自己文件；报告写git-dir/sdd。根同时负责Task2与文档；提交串行。

## Task 2: 普通用户入口

根代理：domain/task.ts允许具有当前three-platform绑定的多选单次任务，保留legacy/跨设备/未知来源限制；显示共享记录上限不足平台数的阻断原因。原生任务详情增加明确确认恢复/继续入口，绑定当前用户/设备/任务原START，并如实显示服务结果与未完成状态。复用现有布局、schema和服务。

## 实施与验证

首轮候选：UI `9900dca`，native `1cce30c20919b49d39ce69b6d9e094c3e5bb7da1`。已实现三平台串行、预算分配和详情确认入口，但**首轮独立审核 NO-GO**，不得以定向测试通过发布：

1. 前缀 FINISH 回执是任务级 RUNNING/false，不能要求整任务 SUCCEEDED 才续接。
2. 本地仅剩 START 不证明未执行；所有 launch 必须核对当前服务端 generation 和完整本机批次，缺证据不能重采。
3. **后续联验已更正的审核误判**：首轮认为execution.task按平台名返回、与原确认顺序不同；实际get_task第536行已经按target_order排序，自 `990ebca8` 即如此。首轮只看到 `_locks` 内部排序，漏看输出排序，不是实际产品缺陷。按platform_run_id映射和乱序合成测试保留为防御性处理，不能宣称修复了真实接口乱序；详见[更正联验记录](2026-09-11-multiplatform-http-acceptance.md)。

首轮有限证据：renderer 两文件29 passed（初始3个缺实现失败，另2个缺boundedRequest参数的实现失败已修复）；native两文件49 passed，预算反例先失败再通过；整树tsc通过。审核同时发现前缀恢复和多批次RECOVER等场景缺测试，修复批须补上。尚未构包，未重复全量/数据库测试；没有真实平台/Windows/生产/UAT证据。完整Goal仍在进行，修复后复审差量再交付。

修复候选 `0dfdaf825fa5b94407217c245a44c419c4fb5da9`：已改为所有launch前读取当前任务、按原平台run ID映射顺序，核对前缀真实平台完成及原FINISH；后缀须generation0且无任何执行记录。首次CLAIM若generation>1也不启动driver，封住查询与领取之间的重采窗口。多批次恢复在写入前整体检查映射。定向native两文件 **58 passed**（与49项重叠，不累加），包含日志缺失、乱序原回执、前缀恢复、多批次、取消、身份变化和第二平台UNKNOWN；反例原先失败已保留。整树tsc通过，未变renderer29项复用。该候选一次renderer构建通过，不是Windows包。

非作者 `multiplatform_once_final_review` 对修复差量复审 **GO / 本批代码**，当时将三个报告项关闭；后续证实R3为上述审核误判，实际确认缺陷为R1/R2。保留两项非阻断测试精度边界：冲突批次用例先命中generation不一致，未单独到达重复分支；monitor预算用例只覆盖单平台，跨平台均分依赖已测once与共享实现的代码审查，不将其写成三平台monitor端到端实测。首次审核和失败证据不删除，未追加无变化的全量测试。真实来源、Windows、部署和客户验收继续未完成。
