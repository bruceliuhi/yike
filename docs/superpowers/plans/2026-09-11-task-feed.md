# 真实采集任务列表

基线 `7f42e1c`。补齐已批准线索采集页面与真实执行库的连接，复用现有布局、草稿、原执行请求和取消机制。不改变完整 V0.2 范围。

## Global Constraints

- 只读 feed 由当前认证身份解析 owner/tenant，不依赖设备 READY；不泄漏配置全文、账号凭据或其他客户任务。
- 复用原 execution tasks/runs/platform runs/operations；列表状态与统计来自同一 SQL 快照，不新建任务模型或伪造旧 TaskRun 回执。
- 取消仍通过本机现有签名 CANCEL 与原请求 journal；须逐任务明确确认，仅本任务所属当前设备。未知取消先核对，不新造请求重试。
- 记录数是已入库采集记录，不是有效商机数；服务端取消与本机来源停止分别呈现。列表读取、跨页和换用户不会触发执行。
- 仅改动路径测试、一次独立整批审核；不重复全套测试/构包。真实平台、Windows、生产及 UAT 仍单独验收。

## Task 1: 后端只读 feed（独立实现 Agent）

仅修改 pilot/execution_api.py、pilot/execution_runtime.py、相关 Python tests；确有需要才新增小型 execution feed 合同模块。不动 desktop 和其他产品文档。

增加 GET /api/ui/execution-task-feed?limit=20&cursor=...，limit 1..50，cursor 可选有界1024字符、格式非法422；同现有认证/HTTPS/runtime不可用处理，拒绝未知查询字段。不接受 owner/tenant/device 等查询覆盖。返回 `{schema_version:'execution-task-feed-v1',items:[item],next_cursor:string|null}`。按 created_at DESC,task_id DESC，keyset cursor 编码时间+task_id，取limit+1；空页是已核实空页。

GET /api/ui/execution-task-feed/{task_id} 返回 `{schema_version:'execution-task-feed-item-v1',item}`，仅当前 owner，非法 UUID422，跨用户/租户或不存在404。

item 字段固定 snake_case：task_id,run_id,device_id,profile_version_id,strategy_version_id,start_request_id（UUID）；name（配置 name，缺失为 null，不伪造）；mode（once/monitor/null，未知为 null）；created_at,deadline_at（带时区ISO）；status（PENDING/RUNNING/CANCELLING/CANCELED/SUCCEEDED）；max_records（1..10000）；records_used（原平台计数之和）；stop_confirmed（与现有 get_task 同语义）；platform_runs 数组1..5，按target_order，元素 platform_run_id,platform,status,execution_generation,records_used。name正常策略最多60字符；历史无合法值返回null。查询必须以原START回执绑定 request_id，不把最近的CANCEL当START。

单条/列表用同一只读 SQL 聚合，避免 N+1 和读取间状态不一致。保留原 get_task DTO 和签名API不变。已有 restricted execution role SELECT 足够时不迁移不扩大授权。

定向测试：真实隔离 PostgreSQL 受限执行角色启动/计数/取消后的值、同租户另一owner与跨租户隔离、稳定分页、单条404；API认证/有界参数/未知字段。使用本批独立临时PG容器 cached postgres:16-alpine，不得操作共享 yike-identity-contract-pg。仅指定 tests，报告给出命令结果与合成数据边界。Python /tmp/yike-main-merge.PZkSlU/.venv/bin/python。独立容器用后删除本批合成卷。提交仅自己文件，报告存本worktree git元数据 sdd 绝对目录，禁止产品 sdd。

## Task 2: 客户端列表和确认取消（root）

共享严格 feed schema 与固定只读 taskFeed.list/get 路由、真实 renderer service；/collection及详情改用真实列表，保留新建和本机草稿。展示名称、模式、时间、服务器状态、入库记录/上限、平台进度、详情与商机复核导航；加载/失败不能显示成零结果。带下一页和返回首页，单条深链接不依赖第一页。

可查看其他本用户设备的历史；仅当前READY设备与任务device匹配才显示可确认取消。useDesktopExecution 增加 cancelTask 复用现有 journal 防重、原取消回执与未知恢复面板；不创建第二套发送逻辑。原有新建页 cancel(entry)保持兼容。本地STATUS查询只读，来源停止未知明确显示；监控轮次取消提示不等于暂停计划。

路由/用户scope变化取消显示旧详情和晚到响应，不把旧任务确认应用到新页。定向UI验证加载/分页/深链、明确确认取消/重复防护/未知核对、晚到响应；适配器schema/路由安全；typecheck。最后冻结源码后一次renderer build，不冒称Windows安装包。

## Task 3: 整批审核与交付

独立 reviewer 审核完整 BASE..HEAD、相关证据与边界；修复只增量验证。短记录更新任务书入口，正常快进推送 main。保留原Goal。
