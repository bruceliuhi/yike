# 客户页面信息收敛：设计与实施记录

## 接续：公开任务实际阻塞定位（2026-09-13）

- 13:37 后续实机：0913B 点击保存后明确显示“本机草稿·未启动”，仅会话保存；连接列表四个原生平台均未连接。小红书连接弹窗正常显示“等待打开登录窗口”，打开按钮可用且没有设备核验阻塞；未替用户登录或点击本人核验。此证据不证明平台已连接。
- 本批最小修复方案：只改 TaskWizard 第二步展示为“采集准备／尚未就绪”，提示检查平台连接及服务，不把不可用能力归因为设备未绑定；不改 info、授权、刷新与启动逻辑。测试先复现旧文案，再验证重新读取可用能力后恢复状态且不自动启动；纳入下一统一包，不为几行文案单独构包。已完成 RED（新标题缺失）和受影响两文件 52 项 GREEN，类型检查 exit0；独立差量审核 review_collection_readiness_copy GO、无阻断问题，审核未重复跑测试。旧实装仍显示旧文案，不宣称实机已修复。
- 同一实装 56e8abd 已采用上轮真实生成的 8 个关键词和策略，新测试会话草稿为“验收-公开需求-AI定制-0913B”，仅选择公开网站；未启动任务或外发。记录数 3/运行 120 秒是采集执行设置，不是研究模型费用上限；研究估算要求先确认策略，未取得报价。
- 原生窗口第二步持续显示 V2EX 来源不可用及“待绑定或检查”；点击一次“重新检查”，再独立读取窗口，结果不变。未复用旧草稿的丢弃授权清除此新草稿。
- SSH 只读核验：生产 app healthy，镜像仍 d9c26f01684a0137e753ddb3ec6a97bb01548b99b218111b333d92fa9f8e7393；明确配置 COLLECTION_MODE=four-platform-monitor-v1，未配置 RESEARCH 前缀变量。该模式的 foreground_collection_support 不返回 public_source；这是当前公开来源不可用的部署原因，不能通过重装或重复身份核验修复。本轮未改生产配置。
- “本机运行环境已准备好”来自 getPortableRuntimeStatus，任务页 deviceReady 则来自旧运行服务或 foreground CAPABILITIES；后者还依赖已授权采集连接。二者并非同一状态。已排除简单刷新问题，但未取得身份/连接链每层结果，不能断言设备身份失败；不要把本机文件准备好直接改成采集已就绪。下一步分别核对真实平台连接及研究部署门禁，并收敛误导提示。
- 本地 main 已快进到 64770a1，包含后续简报时钟偏差修复；安装包仍 56e8abd，不追认为最新源码。仅补现场证据，未重复构包或全量测试。

## 最新实装与真实操作（2026-09-13 13:17–13:25）

用户明确批准放弃“验收-AI软件定制-0913”会话草稿并安装新版后，正常关闭确认已完成，无强杀或手工清用户数据；旧草稿按产品会话语义清除。Setup PID22208/session17171 返回 exit0。实装版本 56e8abd 的 ASAR 385db655…23f5b 和载荷清单 074bd047…23f815 与下方候选完整摘要一致；新窗口进入已登录工作台，13:19 显示本机运行环境已自动准备好，未输入授权码或重新进行身份核验。

- 实机新建页保留已确认画像 v1；公开网站显示“无需账号”。预览仅三行服务/目标客户/地区，空排除项、模型/provider/披露规则及发送长说明均不显示。
- 取消预览的一次即时可访问性快照与后续状态不一致；重新观察并通过实际取消按钮关闭成功，未据此修改产品或宣称已定位前端故障。
- 用已确认的 AI软件定制/全国线上交付画像，仅提交一次搜索建议；真实界面返回“建议已生成”和 8 个关键词。结果不显示建议原因、依据/未知信息长解释及来源代码，更多筛选条件默认关闭。停在结果预览，未采用这次结果、未启动采集、未发送评论/私信；不是新商机或客户效果验收。
- 原文样例详情显示发布者/时间、引用摘录、来源入口、待人工复核及未入客户库；“多找类似”样例预览的创建任务按钮禁用。仅验证样例边界和实机入口，未把样例算真实客户线索。
- 已同步主干到 0b88a8f；相比候选，desktop 仅多两条 broker 未知结果解释（e2855e8），安装包不包含这两条后续文案。未重包或把 56e8abd 称为最新主干包；下一次统一候选纳入。服务端隔离研究的真实成功/回收、平台账号连接、真实采集与联系闭环及签名仍未完成，Goal ACTIVE。下文关闭等待是此前历史。

## 追加：删除模型解释与技术文案（2026-09-13）

用户明确要求删除搜索建议里的模型/provider/披露规则、空排除项、业务介绍发送长说明，以及“建议原因”中来源枚举与模型长解释。本批直接删除这些展示，不换成长篇同义说明；短句/判断弹窗同步收敛。保留业务关键词、非空排除词和采用操作，筛选细节默认折叠；实际描述载荷、确认绑定、费用、原文、收发对象及 UNKNOWN 恢复不变。连接能力中文化、内部编号折叠；多条恢复记录提供短标签并在取消确认中复用。公共网站不再提示待连接，显示无需账号。

- 基线 a16e7b7。新增预览/解释/公开网站用例先 RED 后 4 项 GREEN；连接/错误两项 RED；错误层屏蔽 Zod 和纯技术英文异常，业务恢复提示保持。此规则不是安全过滤器，中文混合诊断仍需后续显式错误类型治理。
- 两组定向分别 84/49 项通过。全 UI 首轮 1442 passed / 4 failed / 0 skipped：三项旧断言期待英文诊断，一项仍定位旧回复按钮。修正断言/业务夹具后受影响 7 文件 66 项通过，不伪称全量重跑通过；新增错误测试保留无技术正文断言。类型检查及 renderer 生产构建通过，原有体积警告保留。
- 独立审核 6 文件 41 项通过，发现多 START 记录取消对象不可辨识 P2；追加双记录排序/取消绑定用例先 1 项 RED，再 3 文件 22 项 GREEN。独立差量复跑 14 项通过，GO，无剩余 P1/P2；候选安装结果待本节续记。
- 原始定向证据保存在本机 .runtime/customer-technical-copy-*.json、customer-copy-final-ui.json、customer-copy-regression-fixed.json、customer-copy-multiple-*.json；不把测试夹具当真实收发。

### 追加删除文案的 Windows 候选

UI 代码 f9bb6e8，冻结候选 56e8abd00c8d5e2fbef83a253a8d8d60d89acfa4 已推 main。随后同步 c22ea0d 服务端来件，desktop/app 无变化，不重复编译，也不追认冻结包为新提交。

- 便携构建/搬迁/独立 Python/Chromium **1 passed / 0 skipped，124.91秒**；清单 074bd047d84fe398e85637d4110e84dc198700c6aa7d91c22b498e623123f815。645 项固定桌面输入前后摘要 ed208d36c236a2679f73ae43374fa8e79ed34f911a79377a8726b53df0a6d474 相同。
- 首次已编译产品，Squirrel 封装因漏设独立 SQUIRREL_TEMP 导致 PathTooLongException。补短目录后 skipPackage 仅重封装，11,157 个资源前后字节相同，exit0；没有重编译或重跑业务测试。原始 .runtime/candidate-source-customer-copy.json 保留首轮 FAILED，candidate-remake-customer-copy.json 记录同字节成功。
- Setup：C:/ykc913/make/squirrel.windows/x64/YikeAI-Setup.exe，642,867,712 字节，SHA256 bc8bbe1624de4826d5901f26dc3bd304b20f838ffd7ce09ac06378bc816440be；nupkg 646,501,490 字节，SHA256 4af47aec8ce67008cff25b571f423e05d5ffd3f94b3894ae522609512f00dc39；仍 NotSigned。
- ASAR 385db6559e5fd8f172e9d04a63cea52fdef590f828e0c0ed499fdd695a923f5b。包内主入口、40 个 renderer 文件与固定构建一致，HTTPS 和载荷 pin 通过；旧 Windows .NET Packaging 实读 nupkg **11,204 parts**，ASAR/清单两项摘要一致。
- 实装仍 a16e7b7。正常 UI 将已有 8 个建议词合入“验收-AI软件定制-0913”会话草稿，未新调用模型/启动任务/联系。退出明确提示会清除会话草稿；尚未获本次放弃批准，未点击放弃、强杀或启动安装器。只读复查确认仍在。下一步用户选择后安装同包并检查真实弹窗；本轮未重新部署服务器，完整 Goal ACTIVE。

### 本批前已完成的同版本交付

实际服务器和 Windows 已更新到 a16e7b7，不再停在下方历史清草稿检查点。服务器健康、重启 0、只读根目录、非 root、HTTPS health/ready 通过；数据库结构及运行配置未变，未启用隔离动态研究。镜像 ID d9c26f01684a0137e753ddb3ec6a97bb01548b99b218111b333d92fa9f8e7393。服务器复用锁定依赖，188 项源码/资源字节及运行导入检查通过；首次 CRLF 归档校验失败后改用 LF 归档，失败产物未部署。启动即刻健康检查尚在 starting，之后同容器检查 healthy，通过，无重部署。

Windows a16e7b7 的 Setup 位于 C:/ykv913/make/squirrel.windows/x64/YikeAI-Setup.exe，SHA256 f594eae7dadc76163afc7286c4e6cf49738e81a8b7c4f6f2942d1e6f2efb19a8；实装 ASAR 947682379fdaa4adb81cb8a58a81cf2aee8464cd4d18ea3811753eeae04ad1ee 与候选一致。便携搬迁/独立 Python/Chromium 1 passed，Node 24.19.0 构包和 Setup exit0；未清登录数据，实际画像仍是 AI软件定制、全国线上交付。实机工作台、画像、新建任务已查看，用户据此提出上述新简化。本段版本不含追加删除文案；签名仍 NotSigned，真实平台联系/客户闭环及生产研究待验，完整 Goal 不关闭。

## 用户要求与设计

2026-09-13 用户指出行业策略小字、补证顺序、停止条件及执行保护上限不应给普通用户增加负担，随后要求其它页面一起排查处理。依据 AGENTS.md 的局部 UI 自主细化授权，本批不重新生成设计图或重复索取设计确认。

采用渐进展示：主页面只保留任务目标、必要输入、结果与下一步；专业参数和规则版本收进默认关闭的“高级设置”或“查看详情”。纯执行教学（补证顺序等）移出客户页面。既有八入口、布局风格、字段值、确认摘要和接口不变。对比全删参数会损失修复入口、重建简易模式会增加维护成本，沿用现有组件折叠最适合本批。

费用/用量确认、当前账号状态、原文与时间、证据不足、失败/结果未知、取消及发送对象/内容确认不得隐藏或弱化。高级字段如有错误，主层显示可行动提示，不能出现不可见阻断。折叠不得清空已编辑配置或使旧确认有效化。

## 实施计划

- [x] 根任务：新建任务及 ResearchSettings/IndustryTaskStrategyEditor/StrategyExecutionLimits，先写显示层级及保留校验测试，再修改。
- [x] 页面组 A：工作台、画像、账号/连接/登录的源码排查与定向修复。
- [x] 页面组 B：商机、触达、跟进的源码排查与定向修复。
- [x] 页面组 C：任务列表、监控、研究进度及确认详情。
- [x] 检查页面组差异，定向测试与类型检查；一次独立需求/质量审核，问题仅差量复核。
- [x] 完成下述覆盖边界内的可见页面验证、展开/错误组件测试及固定候选构包；不把源码通过写成安装版通过。
- [ ] 新候选实装页面核对：退出旧版遇到清除会话草稿确认，等待用户选择，未丢弃或强制退出。
- [x] 合并并推送 main，唯一任务台账链接本记录。未验的生产研究、本人平台认证、签名及真实客户闭环仍保留。

## 证据与覆盖

实施前：Windows 已安装 3ffddbd 的新建任务实际页面显示行业策略长说明及右侧补证顺序、停止条件、计量规则；任务条件需要滚动才能看到下一步。源码基线 963b0b9，安装包与源码不同，不能互相代替验证。

其余页面的源码排查不冒充真实数据状态或完整无障碍验收；后续在本节补充实际覆盖与未完成项。

### 全页排查结果

- 工作台：检查口径/全部待办已有折叠，保留未完成检查、样例与客户数据区分；本批不改。
- 业务画像及资料：折叠重复画像摘要；确认弹窗摘要、资料授权、覆盖影响及错误保留。
- 采集/监控/研究：收起编号、版本、重复教学；行业策略及资源参数可展开，错误在外层可见；原文与进度、取消/未知恢复保留。
- 商机库/证据：观察池解释收起，来源版本/哈希/核验编号收起；中文判断代替重复英文枚举，原文作者时间与风险保留。
- 触达/跟进：队列和关联回复编号收起；收件对象、渠道、草稿全文、发送确认、实际回复及未知结果不变。
- 账号/连接/登录：连接版本移入详情、账号和设备归属保留；既有诊断弹窗和可选登录方式沿用，不修改本人认证步骤及权限。

### 验证与审核

- 根任务组件 3 个预期显示失败、1 个输入定位歧义及 1 个错误透传通过保留在 `.runtime/customer-task-simplicity-red.json`；修正测试定位并实现后，5 文件 78 项通过（`.runtime/customer-task-simplicity-regression.json`）。新增用例验证默认折叠、展开、收起后保留编辑、外层错误及不自动采用执行参数。
- 全 `desktop/tests/ui` 首次 **1419 passed / 1 failed / 0 skipped**（`.runtime/customer-pages-ui-regression.json`）。唯一失败为策略测试沿用“执行保护上限”旧 summary 标题；只修正定位后该文件通过。此记录不冒充全量重新运行通过。
- 独立审核发现同画像多监控收起编号后不易区分：新增行内可展开计划详情，操作确认直接显示完整目标编号和周期；双计划 ACTIVE/PAUSED 两项 RED 后 GREEN。独立差量复核监控与策略 2 文件 **25 passed**，P2 已关闭；其余差异 GO，无新增 P1/P2。
- 独立初审范围指纹 `e83338ac0a043ef50bf482fe4e5a89e5d6a8adc75edb5fdbe41e49eea1097d79`；差量 NativeMonitorPlans blob `6d9f46c273e8740f5ce06d9b9832fa5a0a901123`。
- 类型检查和 renderer 生产构建通过；Vite 原有大块体积警告保留。页面组定向用例与整组有重叠，不累加为额外测试数量。
- 既有测试漂移单独说明：候选 URL 测试补既有 `evidenceVersion=1`；损坏草稿测试改独立测试用户，避免清草稿 tombstone 影响；生产服务与草稿保护均未修改。

### 可见验证边界

- 当前安装版 3ffddbd：原生查看工作台、画像、采集空列表、监控空列表、商机空库、触达空状态、跟进空状态、平台连接入口；确认画像重复摘要和观察池常驻教学问题。没有真实新任务/发送。
- 新源码通过既有 `tests/visual` 隔离入口在内置浏览器渲染（合成状态，禁止外部请求及客户数据写入）：新建任务不再显示补证顺序和停止条件；画像摘要折叠；商机观察池说明折叠；原文证据和联系草稿仍可见。确认页实测发现重复技术说明，追加同批收敛，不以此前截图当最终状态。
- 真实账号下没有候选/回复数据，详情填充状态由合成夹具验证；不声称真实客户闭环或完整无障碍合规。
- 确认页追加差量：处理范围用普通话且保留准确条数/秒数；未设明确返回配置；技术停止条件改短句，不改变费用/确认。新两项 RED 后通过，独立复核 2 文件 21 项通过，GO；组件 blob `444438d7273da6a35c5a501e2643b6d4a7625cf0`。内置浏览器刷新确认最终文案已生效，任务尚未启动。

### 固定 Windows 候选

- UI 提交 `d49751c`，与来件 `db95924` 合并后的 `main` 为 `ff985d97c99612ef262be808adff7d6ea6072177`，已推 Gitee；任务书冲突保留双方记录。冻结干净 LF 工作树 `.worktrees/win-customer-pages`，服务器未因本批 UI 改动重新部署。
- 内置浏览器另核对平台连接、监控列表、触达编辑、跟进填充状态和设置页；均为隔离合成夹具，不算真实采集或发送。
- 新便携载荷真实构建、搬迁及独立 Python/Chromium 检查 **1 passed / 0 skipped，130.01 秒**，原始 `.runtime/portable-customer-pages-valid.xml`。清单 SHA256 `26750711b4a848991faf3a31c87ef0ac4f77d904003bb8d19ee3d5c9b111e340`；首次错误源路径被构建器拒绝，纠正输入后通过，没有修改门禁。
- 构包首轮误用 PATH 中 Node 24.11.1，发现不符合 engines 后中止，不交付该轮产物；改用既有 Node 24.19.0 后完整 make exit0。固定源码在 Node 24.19.0 下类型检查通过。
- 最终安装器 `C:/ykui913/make/squirrel.windows/x64/YikeAI-Setup.exe`，**642,869,248 字节**，SHA256 `d29569073b75de23798bd3889dc029ada642259214b726cb8db8a5923f8a2d3a`；nupkg 646,525,695 字节。签名仍 `NotSigned`，不作为正式上线通过。
- ASAR SHA256 `06e811106038687000ab62fee1b91968fd14ddb03733725f2c87bcfa7e8bc258`，实际读取主入口、renderer 资源及内置 `https://yike.tuokexing.net`/固定载荷清单 pin 成功。最初检查使用 POSIX 分隔符导致读入口报错，改用 Windows 路径后确认存在，没有修改产物。
- 实装前核旧 ASAR 仍为 `098ef7a5873b9b5aa63e5b6ba387b54796b4b6e33844cf37410b71976cbd37ba`；平台页真实显示本机已准备好。正常 Alt+F4 出现“尚有未提交的更改 / 关闭窗口会清除会话草稿”，未点击放弃、未强制结束或启动 Setup。当前实际客户端仍是旧版，待用户选择后继续更新，登录数据未清理。
- 自动接续只做无损检查：Windows 旧 .NET `System.IO.Packaging.Package` 实际读取最终 nupkg 的 **11,204 parts** 成功，包内 ASAR/载荷清单 SHA256 与上述固定摘要逐一相同。不是仅 ZIP 可读，也不替代 Setup 实装。再次只读核对旧版窗口，清除草稿确认仍在；没有用其他工具绕过该选择。
- 同轮线上只读核对：HTTPS `healthz/readyz` 分别返回 `ok/ready`；实际 customer 仍为 `e1bee5aaddff0676592c2fa103ca61ed0e954aa9`、healthy、重启 0、只读根目录、用户 yike。没有部署或启用新研究，线上健康不证明尚未接通的隔离研究及客户闭环可用。
