# 买方检索与受阻来源切换

## 实际失败与设计

基线6cddea0，已部署服务b30415f/146，Windows实装1549c18。UI任务86d3271d-4880-43a6-8530-af3582cc9e5d（验收-AI定制原文闭环-0913）于17:31:49创建，17:33:22结束STOPPED/no_verified_reads：6 MODEL、7 SEARCH成功，2 READ均FAILED/connection_unavailable，原文及候选0。第一次失败后仍有成功SEARCH，没有旧SQL异常或遗留ISSUED；旧失败任务不重放。

只读检查实际付费搜索回执：7个查询均含AI品类表达，主要返回供方推荐、资讯和旧技术讨论；唯一读页主机www.v2ex.com失败后又选择同主机。当前customer容器内只做DNS/TCP诊断（无HTTP/模型）：V2EX首IPv4连接3秒超时，IPv6无路由；知乎及另一已返回网站的IPv4可以连接。这是本时点连接差异，不声称其它网页正文可读，不修改DNS、代理或绕过访问限制。

原规则包search-and-coverage.md包含具体买方业务表达和独立社区路径；efficient-handoff投影仅保留抽象原则和V2EX目录提示，模型没有收到原文示例。保留紧凑投影，补齐业务对象+交付物/问题+寻源动作、仅匹配业务时的少量示例、独立来源探索及受阻后不重复同站路径。示例不是硬关键词、来源许可或买方证据；非AI客户仍按自身画像展开。

不选整包全文回灌（恢复无关阶段职责与输入负担），不选增加预算/强制读页/换DNS代理（未解决查询质量且可能误读或越界）。本批选择恢复原有研究方法中的缺失内容，不增加功能、放宽证据或禁止合法提前结束。原规则包不改，投影随现有rule_sha256绑定，输入/输出schema不变。

## 实施与验收

按用户既定自主设计授权和单批审核要求，串行main；合并设计/计划/代码审核，不追加逐项用户确认。

- [x] tests/test_research_context.py增加实际compile输出的合同回归，先验证缺失失败；检查条件化示例、来源切换、无绕过、长度及原业务边界。
- [x] pilot/research_stage_rules.py最小补全投影，现有context摘要继续覆盖精确文字；不改网络、账本、预算、候选阈值或旧任务。
- [x] 定向测试及一次独立审核GO，提交main；真实效果另验。
- [ ] 同源服务候选部署后，从已安装客户端新任务检查实际查询/独立来源/原文与候选；不把文本合同测试当模型质量证明。无本地运行差量不重复构Windows包。

完整邀请试用仍需原文→候选→草稿→反馈、三业务及用户认可的真实候选；本次0候选不能说市场无需求或可以公开上线。

## 定向验证

本机默认.venv启动遇.pth编码错误，系统Python无pytest；不重装环境，使用已存在`.runtime/venvs/win-dev/Scripts/python.exe`：新增编译合同RED 1失败/60未选（缺业务对象表达），GREEN context 61通过/0.33秒。pytest清理旧临时目录时有权限警告，未删除或修改那些目录。worker是POSIX入口，Windows跳过不算通过。

隔离Linux研究镜像使用最终stage文件只读覆盖、最终context和同版worker测试、无网络/客户挂载/模型密钥；65通过/64未选、1.35秒。覆盖全部context与4个worker指令接线用例。首个容器测试夹具因只读父挂载缺tests路径启动失败，改为独立测试目录及单文件产品覆盖后通过；没有变动生产运行容器。本结果证明指令投影与接线，不证明模型已实际采用新策略或产出线索。下一步是单批审核、同源服务候选及客户端真实任务，不重复此组测试。

独立review_buyer_discovery合并方案/代码审核GO，无阻断项；stage Git blob `309a32e9ce3846736740d1273d8053fa23682f5d`、test blob `9b2cbeca46e0bd78efbbdf48e5b1bc6e1e19c5a3`。复用上述定向证据，没有重复测试或构包；目前尚未部署此指令修复。
