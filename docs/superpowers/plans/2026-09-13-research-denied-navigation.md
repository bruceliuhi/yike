# Denied navigation is not corrupted evidence

## Real research boundary

New actual-provider task at `d514da5`, task64dec995-4269-4dad-9ac6-d017b9b003ff / run3eda9abf-2f22-455a-ac32-62139ef2e884. Explicit native-recent-list hypothesis, unchanged 8 sources / 8 model actions / 3 minute bound. In26.02s: one SEARCH succeeded, two MODEL succeeded, next MODEL UNKNOWN; zero READ/accepted/analyzed. Mission invalid_runtime_output; no upstream call observed for the final local408 after shutdown. This is NOT a completed source-quality trial and does not prove the earlier admission fix failed. No retry of that UNKNOWN. Evidence `/tmp/yike-native-buyer-20260913.json`; owned disposable PostgreSQL stopped. Search query was `V2EX 外包 节点 最近项目`, returning nine indexed entries including root/tag pages and stale topics; none was read or qualified.

## Independently reproduced protocol mismatch

Actual MCP and host deny undiscovered URLs before reader/network I/O with exact `{status:FAILED,code:invalid_url,replayed:false}`. The event observer rejected that legitimate denial as invalid runtime output solely because the URL was undiscovered. Two new regression cases were RED. This is a confirmed independent defect consistent with the observed failure, NOT proof of the precise original offending event: raw event payload was not retained by that probe.

Only this exact denial can now be recorded without aborting. It does not enter successful reads or grant future URL admission. Unseen successful/ambiguous frames, altered schema, fake replay, context/authority violations and durable UNKNOWN remain blocked. No network/authorization/limit change, no migration, no customer protocol expansion. Actual MCP denial is fed into the observer test; unknown successful evidence still rejected; allowed subsequent evidence still accepted.

## Remaining work

No new buyer opportunity. Research routing/selection quality still unverified; copying more keywords cannot compensate for runtime protocol failures. Preserve exact failure diagnostics in the next new-task probe before any further paid run. Then validate recent native reading and distinguish background/list pages from selected buyer evidence. This small correction is not completion of the full V0.2 product or recovery of the real UNKNOWN.

## Verification

Code `d40fce00f6b7ecd325c8186c8226e8b4042ced25`: initial two cases RED; worker/navigation/gateway78 passed26.94s. Added exact-shape negative cases and fed an actual in-memory MCP denial into the observer; navigation18 passed0.48s. Protocol fixtures are synthetic, not platform collection or real buyer results. No PostgreSQL restart or further real model run after the recorded failed task.

Independent `admission_limit_review`: Spec PASS / Quality PASS, no actionable P-level finding on exact SHA; worker/navigation77 passed18.05s and diff check clean. MCP/host denials confirmed before I/O; successful/ambiguous/altered/replayed unseen frames still invalid. Report `/tmp/yike-denied-navigation-review.md`. No actual-runtime root-cause claim beyond the independently reproduced observer defect.

## New real task after the correction, then local Skill source comparison

New task at`fbb4d79`: task06090295-e32b-44af-ae96-0f2a26ff61c2 / rund85696a4-78e5-460a-9fe6-d3b948740fa5,55.72s,2SEARCH/1READ/5MODEL succeeded, anotherREAD UNKNOWN. Final STOPPED/effect_unknown, accepted/analyzed0. Source evidence`/tmp/yike-native-shape-20260913.json`; safe invalid-event-shape diagnostics`/tmp/yike-native-event-shapes-20260913.json` is empty. The run recorded and passed an undiscovered LinuxDO URL's invalid_url rejection, demonstrating this correction on real execution. The later `https://linux.do/tags` read returned access_restricted and closed the durable dispatcher as currently designed; no platform bypass/retry. This run did not reach source-budget exhaustion or ordinary assessment. All five successful model calls reported usage; the subsequent call was locally refused, not counted as a successful provider call. It is not completed research or a quality A/B trial. Owned disposable`yike-native-shape-pg-20260913` stopped/absent.

The model chose V2EX `/tag/外包` (readable, mostly workforce/outsourcing discussion), not the known `/go/outsourcing` project node. To check that difference without more paid model calls, local Skill independently read that known public node's first two pages (40 title cards) and two linked originals; these reads are not customer pipeline receipts. Four successful local reads saved under `/tmp/yike-local-outsourcing-20260913.json`, `/tmp/yike-local-outsourcing-page2-20260913.json`, `/tmp/yike-local-calibration-20260913.json`, `/tmp/yike-local-paid-program-20260913.json`. A linked Feishu brief returned unavailable; no login, retry or alternate interface.

Qualification: known industrial calibration V2EX1234774 has actual quote invitation, but brief/onsite/value concerns require B+/REVIEW, not new qualified supply. MCP V2EX1230884 has vague paid intent without buyer business scope, OBSERVE; third-party company-MCP comments cannot fill it. Current queue records source/date/strongest counterevidence/contact and history limits. No SEND_READY, production changes or outbound.

Next action is source-entry strategy, not another global keyword expansion: distinguish project nodes from same-name topic tags using actual page content; carry known blocked paths into the next task; retain unselected research/list pages separately from buyer candidates. Known access restriction stops that path; never relax it to claim availability. The successful local node read does not prove the product autonomously chooses that entry. Reader's50-link cap also omitted later topic links behind navigation/member links in these list pages; record as coverage limitation, not fully traversed40projects.
