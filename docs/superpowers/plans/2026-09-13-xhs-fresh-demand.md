# 小红书新需求与原帖保留（2026-09-13）

用户已授权独立推进；本批沿用直接 main、小批定向验证和一次候选构包。

## 实机问题与边界

a6a2dae 实机已完成 10 条采集、原文入库及一次真实 AI 判断，但多为旧热门教程评论，不代表有效需求。固定运行时默认 popularity_descending；普通小红书输出读取器只返回评论，丢弃原帖作为独立候选。

## 本批任务

1. 在现有受控采集启动入口将小红书搜索默认设为运行时已有的最新排序；不新增用户配置，不修改登录、账号守卫和平台限流。
2. 小红书原帖以 POST 候选保留真实正文（无正文可用原始标题），同时保留 COMMENT 候选、原文链接、作者和真实发布时间。采集观察时间不能冒充发布时间；不得自动确认来源或客户价值。
3. 原帖和评论共同遵守现有总条数预算，包含预算为 1 的情况。调整固定采集参数及读取契约使正常输出不过量，不静默截断损坏数据。不扩大其他平台范围。
4. 定向 TDD 验证上述路径与相邻预算、映射、账号守卫回归；一次独立差量审核后提交。构建一次 Windows 候选并安装，实机使用更明确的采购需求词验证新鲜原帖是否进入候选，再继续判断、草稿和反馈。

## 状态

- [x] 修正实现及定向验证
- [x] 差量审核（主代理 Spec、独立 Quality GO，无新增 P1/P2）
- [x] 提交与同步：70b1bd6 已推送 main
- [x] Windows 候选构建、安装及字节核对
- [ ] 新版真实采集与后续链路（任务表单已填写，尚未启动）

上线缺口保持：尚无用户认可的值得跟进线索、尚未完整跑通草稿及反馈、未完成不同业务连续试用；旧 927 失败任务取消状态残留另记，不因本批成功销项。

## 实施证据

4 个 Python 产品文件：受控启动设置已有 LATEST 排序；原帖/评论共同分配条数并入库；POST 保留真实正文、链接、发布时间，未知作者不使用内部哈希替代。桌面及服务端已有 POST 契约，不修改界面或部署服务器。

定向 RED 115 失败 / 15 通过；修复后同选择 GREEN 130 通过。相邻 9 文件回归 600 通过 / 3 本机符号链接跳过 / 1 裸 BASE Python 隔离子进程缺 idna；主代理另用实际 portable Python 的 -I 模式导入当前映射器成功，app/sqlite3/psycopg 导入集合为空，未更改无关测试或运行时。旧 XHS 输出夹具按有原帖的契约更新后差量 12/12 通过，非法 ID 用预算 2 验证，避免被新条数限制掩盖。上述均为离线/受控验证，不是新有效线索的实网证明。

独立审核复核上述 4 个产品文件及 5 个测试文件，无新增阻断；整数分配可能少用若干条额度，属于上限而非必须填满。裸 BASE 隔离测试失败保留，不计入通过项。

## 70b1bd6 Windows 候选与实机状态

- 安装包：`D:/ykn913/make/squirrel.windows/x64/YikeAI-Setup.exe`，642856960 字节，SHA256 `e5b6ec329dfe35120e800f6d3ae454c09537d2b5b55b4bfc3aed066ef788ac2d`，未签名。
- portable 实际运行时检查 1/1 通过（117.79 秒）；Forge 构建 exit 0，安装 exit 0。源代码固定 `70b1bd6e2e0848d9a3d7a5d5454f55663b7dd053`；651 个桌面输入构包前后散列一致。
- 候选及实装 ASAR 均为 `8d07fb8ad778546804c700afbfffbf660819a71870ee938febfb62cbf59208b1`；portable manifest 为 `7f4b28ce788cb22cf3f683a6bc12d76c9b53cf726646eff23f39bfa383e7b0e3`；40 个 renderer 文件、主进程 HTTPS 地址及运行时绑定通过。服务端未改动，不重新部署。
- 新版启动后保留产品登录和小红书连接。在实际窗口新建普通任务 `验收-近期原帖需求-70b1bd6`，沿用默认已确认画像，填写“找人开发、AI项目外包、求推荐开发团队”，仅选择小红书。计划 10 条 / 300 秒；当前仍在记录上限输入处，尚未填写上限或确认启动，不能据此声称新排序实网通过。
- 窗口控制随后连续两次 `failed to activate captured window`；刷新窗口列表确认客户端仍在，操作系统报告进程 Responding=True。已停止 UI 输入避免误操作其他窗口，待恢复窗口前台后继续同一草稿，不新建重复任务、不重构包。旧测试会话草稿此前按用户授权关闭放弃，已入库原文及任务记录保留。
- 新鲜买方需求、草稿及反馈、多业务试用仍未验收；Goal ACTIVE，不宣称可对外上线。
