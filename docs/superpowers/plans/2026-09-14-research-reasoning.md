# 研究推理参数对照

目标：修正已确认画像动态研究一律minimal的执行配置，并通过后续真实客户端任务验证是否改善发现质量。不是关键词/协议扩建。

证据：23c35558首MODEL实际reasoning.effort=minimal，input9814/output65/reasoning0；输入买方指导完整。当前服务器V2EX唯一IPv4三秒连接超时、IPv6无路由，cnblogs/eastmoney IPv4可连接（未取正文）。不能用增加同站重试或假定规则漏传解释。官方深度思考页面本次无法加载，尚未据其确认当前模型low行为。

设计：只对controlled且search_enabled且带research_instructions的动态研究设low，其它不满足条件的模式仍minimal。这不是按任务创建时间隔离：同形compiled v1/broker任务首次执行也会使用low；不重放已执行的旧任务。保留模型、工具、确认上下文、调用次数、请求时限、搜贝报价和未知结果门禁；不添加赠送调用、不充值。低档推理可能增加单次token和延迟，依旧受现有执行上限约束；同调用数不表示同供应商成本。不承诺所有查询都会产生推理token。

替代方案：继续提示词堆叠已有实际失败；结构化计划尚无质量证据且有四项合同缺口，暂缓。先做本单参数对照，失败不盲升high或换模型。

- [x] 纯命令配置测试先RED：1失败/3通过，只有受控动态研究为low；保留重试关闭、隔离和输出schema。
- [x] 最小实现、定向测试、一次独立差量审核GO；broker实际传参满足条件。Windows配置/bridge共46通过2失败，失败为超大请求连接中止与滴流计时阈值；未修改无关bridge。Linux原研究镜像隔离覆写最终worker及测试，4配置+2失败用例共6通过/1.50秒。初次隔离容器缺pytest，复用既有只读testlibs后通过，无新增下载、模型调用或生产变更。
- [ ] 审核后服务候选部署，使用最新Windows版本进行新任务实测；检查实际参数/用量、成功原文和买方质量。未完成前不标发布就绪。

本计划按用户已批准自主技术细化与压缩重复审核约定，设计/计划/验证集中记录。
