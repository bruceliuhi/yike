# 历史执行请求展示精简

依据用户已批准自主细化、所有页面隐藏技术细节、压缩重复审核的要求，设计和实施记录合并。

目标：新建任务确认页不默认展开多条历史请求；不删除记录，不改变执行权限和防重。

设计：仅修改DesktopExecutionRequests展示。加载失败/错误提示保留在折叠外。历史明细及原有刷新、查询、重试、恢复和取消控件放入默认关闭的原生details，summary为“历史任务处理”。现有事件处理器、身份检查、disabled条件和数据生命周期原样保留。无需额外状态机或后端更改。完全删除会丢失恢复入口；只改文案仍留下26条长列表；选择折叠。加载中/失败时仍能展开刷新，不隐藏阻塞。

验证：组件测试验证默认收起、可展开且查询不重试、加载失败可见并保留刷新入口；旧执行相关测试与typecheck定向运行。测试先失败再修改，最后一次独立差量审核。暂不重复构包，后续产品批次统一构包，不声称当前EXE已含本改动。

审核补充：记录不全部等于历史完成。存在记录时折叠外保留中性短句“有任务记录可核对，请展开查看处理状态。”；loaded且blocksStart时另显示“当前任务已有请求，请展开核对后继续。”。不自行推断完成/失败，不因收起清空确认状态或调用恢复。测试覆盖有记录的短句和当前阻塞警示，即便error为空也可见。

- [x] 设计/计划检查及测试先失败：补外部阻塞提示后Approved；3失败/14通过。
- [x] 最小实现、定向测试、独立差量审核：8文件97项通过，tsc退出0。Spec/Quality均GO，源码blob c042eb3e0e357f021221c315e531a00e9af07d5c。只涉及组件及execution-recovery-copy、foreground-collection、task-desktop-execution、task-details-disclosure四测试文件。
- [ ] 提交main，保留未通过的真实研究与安装记录。

当前工作树另有并发研究/原文/工作台文案改动，本批不纳入提交，不覆盖。默认收起由原生details.open=false验证，展开使用summary点击；jsdom角色查询不排除closed details后代，未据角色查询宣称实机可见性验证。安装版仍b95b788，本批未构包。
