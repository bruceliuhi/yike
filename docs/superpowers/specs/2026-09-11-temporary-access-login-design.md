# 已批准：受邀临时访问码登录

2026-09-11 用户在侧任务明确“可以 按这个推进”，侧任务 `01a08e85-c239-7071-8af4-21d7426c016e` 转交根任务统一开发、提交与部署。此授权取代旧“必须先短信”的临时试用限制，但不降低隔离、撤销和真实验证要求。

## 用户路径

运营登记受邀客户，在后台明确选择签发临时登录访问码，人工通过微信分发。软件提供“临时访问码登录”：仅输入码，不填手机号、短信或密码。首次成功登录起固定72小时试用；再次登录进入同一用户/租户，不重启试用期。正常重启保留服务会话，但每次访问仍检查试用到期和停用。无公开注册，不新增密码或微信OAuth体系。

既有后台登记手机号可保留，作为运营预登记而非已验证身份；客户不需要在访问码登录表单填写。已有两个短信失败账号只能经后台显式改发临时码，不静默激活。普通短信试用码与临时登录码用途独立，历史码不能绕过短信。后续同一预登记手机号真实短信验证可登录原账号，保留原数据与到期时间；未验证不可显示已验证。

## 最小实现选择

不把旧试用码直接当认证，也不新建一套账户系统。复用 trial、tenant、user、受限 session、后台与现有私有 phone HMAC secret；增加显式凭据用途、独立摘要域、真实手机号验证记录及访问码限流。新码至少192位随机熵，只返回一次、只存摘要，不进URL/日志/客户端普通存储。后台显式改发使旧码失效，不自动创建重复用户。

客户 `POST /api/ui/auth/access-session` body `{access_code}`；只走HTTPS、既有Origin约束，返回现有session公有视图和HttpOnly会话cookie。错码/过期/停用统一access_auth_failed，限流auth_rate_limited。无有效配置明确不可用。客户端固定IPC操作 `session.loginAccess`，秘密只在表单内存和受保护HTTPS请求中，持久化仅复用现有安全会话机制。

实现核查补充：现有 Chromium service partition 实为内存会话，不能支持重启保持。沿用内存partition，额外在主进程使用既有 OS safeStorage（禁止 Linux basic_text）加密保存固定服务origin的 HttpOnly/Secure/SameSite=Strict pilot_session，不保存临时访问码。登录前清除旧会话，成功后先安全落盘再回报；失败明确不能保持登录，退出同时清除缓存。启动恢复仍由服务器核验到期/停用，不能以本机记录替代身份。未配置加密不得写明文。

首次激活与码核验在同一锁定事务；再次登录不延长expires_at；签发与认证来源明确记录。正常受限客户权限与 `pilot_trial_allowed` 必须保留，历史会话在到期/停用后不可用。访问码登录会话有效期不得超过剩余试用期限；退出仍走原撤销。

## 验收

真实隔离PostgreSQL+受限角色：显式后台发码→首次认证/72小时→同码并发及再次登录不延期→错码/限流/过期/停用拒绝→两账户隔离→历史试用码拒绝→短信路径保留/原账号真实验证码回归（合成供应商，不发送SMS）。客户端固定桥接、表单、重新确认会话、持久cookie恢复以及错误处理定向测试。整批独立审核后提交main；部署单独绑定版本、备份回滚和真实浏览器/实际Mac包验证。Windows环境仍缺，不宣称Win包或全链路完成。
